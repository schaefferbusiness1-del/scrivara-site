# ARCHIVE INTEL BRIEF — every proven MLS Assist technique mined from MLS_EVERYTHING

Generated 2026-07-15 by a five-lane parallel mine (handoffs, v1.55/v1.57 sources, v1.98/v2.01 sources, prep-dir evolution notes, reports/builds). PHI-free. For both agents.

---

ARCHIVE MINING BRIEF — proven live-verified techniques from `dispatch-work/*.md` (all reads read-only; patient names/DOBs redacted to shapes; Adam Schaeffer #7833832 retained as the documented designated test record).

## 1. DAY-GRID NAME + DOB (+MRN) EXTRACTION — what actually worked, per era

**1a. DOB lives INLINE in the row text on legacy/classic day grids — as an age-chip pattern, never labeled.**
- Exact live row shape (2026-07-09 grid): `"3:20 PM 20min <First Last> 51yo F | 07-16-1974 <Reason> Check-out …"` — time, duration, name, `NNyo M|F | MM-DD-YYYY`, reason, STATUS all in one row text. Source: `dispatch-work\extension-v159-prep\WHAT_CHANGED_v164.md`.
- Canonical chip regex family used everywhere: `NNyo M | MM-DD-YYYY | #id` (banner adds `#<MRN>`). Age+sex must be stripped **as one unit** or a real middle initial M/F is destroyed: `/\b\d{1,3}\s*(?:yo|y\/o|yrs?\.?|years?(?:\s*old)?)\s*[MF]\b/gi` (CODEX_V2910_SHADOW_PARSER_REVIEW.md, lines 60-61).
- Receipt: v1.6x era reader attached DOB to **44 of 52** appointments on the day grid (rest were OPEN slots) — `HANDOFF_2026-07-09_EXTENSION_OPENER.md` §5. v1.51 era: bridge reply `{ok:true, emr:'athena', frames:7, apptCount:38, withDob:30}`; **DOBs existed ONLY in the structured `appts` array — the page text blob (2,808 chars) had ZERO DOB-format dates** (`dobInText:false`). Source: `dispatch-work\live-verification\ISSUES.md` ISSUE 2.
- DOB attachment in background.js is a separate step `mlsAttachDobs()` consuming the already-parsed name at `background.js:738-760` (v2.9.5 anchors) — a malformed name breaks same-row DOB association. Source: `CODEX_CLAUDE_FREEZE_HANDOFF.md` E1.

**1b. v2.9.21 legacy day-grid selectors (the 18/18 DOB pass on July 14):**
- Rows: `[class~="filled-appointment-row"]` inside `[class~="appointments-container"]`; **no text-length cutoff** (v2.9.20's generic fallback discarded a >300-char row → fail-closed 17/38). Grid renders the day list TWICE (sort-by-department + sort-by-time copies) plus `OPEN` capacity rows: live 40 rendered rows = 2×18 patients + 4 OPEN. Dedup duplicate sort/layout containers; exclude OPEN; keep unresolved rows in the completeness denominator (fail closed). Receipt: **18 unique patients, DOB 18/18, app receipt "added 1 / enriched 17"**, commit `bd37e9e`. Source: `dispatch-work\release-b273-integration\FABLE_EXTENSION_HANDOFF_2026-07-14.md` §3.
- CRITICAL CONTRAST for the current 0/17 blocker: the **Wednesday 07-15 grid variant renders NO DOB/MRN at all** — captured raw grid: 2,606 chars / 17 rows ≈150 chars/row (vs Tuesday's 300+), zero 19xx–201x dates anywhere; rows carry only name/time/reason/**appointmentId (8-digit, from a data attribute)**. So the fix is not extractor markers — it's the row→open-chart→banner identity lane (see §1f). Source: `CLAUDE_CODEX_EXTENSION_SYNC_2026-07-14.md` entries 09:34/09:47.
- Modern scheduling-calendar (GraphQL chip) grid has **NO DOB anywhere** (`patient:{id,name}` only), truncates names ("Virginia W."), and virtualizes both axes. The surface that DOES show full name+DOB per appointment with no clicks is the **Clinical Inbox provider agenda** `/NNN/N/ax/dashboard` week view ("78yo F | 03-30-1948" per row). Also: capture React fiber `memoizedProps.appointment.patient.id` → send as `patient_external_id`. Source: `mls-full-site-backend-qa-fix\task-1-athena-full-month-pull\EXT_V149_DOB_REQUIREMENT.md`.

**1c. Schedule reader strategy lanes (background.js, v2.9.x anchors from Codex audits):**
- Lanes in priority order inside `mlsSchedDomInline()` (begins ~`background.js:2594` in v2.9.5): **structure-id** (`_nmS()` ~2621, `_collectS()` ~2632, keyed by DOM button `id` in `_byIdS`, ~2657-2664), **coord-scroll** (`_seenA[provider|time|name]` dedup, 2692-2696), **inline table-column** (direct Patient/Name column cell `tx(cells[ni])` — BYPASSES all name parsing, ~2719), **grouped/text** (~2706-2735). Legacy fallback `mlsProv`: `RE_NAMECOMMA` at 1928, `patientNameFromRow()` 1963-1976, text rows 2000-2003, table-DOM 2052-2055, grouped-DOM 2081-2088. `mlsMergeSchedule()` 2104-2123 picks by provider presence, NOT name quality. Live diag from 2026-07-09: `strategy:"grouped-dom", rowsScanned:65, tables:2, apptCount:52` — all rows in DOM, **NOT virtualized** on that variant (no `PatientAppointment_appointment-container` elements existed). Sources: `CODEX_CLAUDE_FREEZE_HANDOFF.md` E1 (+2.9.8 anchor update), `HANDOFF_2026-07-09_EXTENSION_OPENER.md` §2c.
- v2.9.9 merge-level dedup at `background.js:2113-2143`: live 50 rows → 25, `providerDiag.dupRowsRemoved:25`, OPEN slots intact, same-patient-two-times and two-providers preserved. Source: `EXT_V295_QUIET_PULL_HANDOFF.md` (~19:00 update) + Codex validation in `CODEX_CLAUDE_FREEZE_HANDOFF.md`.
- Frame exclusion (v2.9.8): stm.esp / coordinator / messaging / letters frames hard-excluded from schedule scraping at 3 layers (per-frame reader returns empty, DOM parser bails, scorer −500) — a staff-message thread had been parsed as a phantom 4:00 PM appointment. Source: `EXT_V295_QUIET_PULL_HANDOFF.md` ~18:15.

**1d. Canonical name parser (full proven code):** self-contained `parsePatientName()` — suffix/particle/nickname/ALL-CAPS/status-metadata aware, returns `{name,first,middle,last,suffix}`, rejects `City, ST`, `Provider, MD`, one-letter given tokens, `DOE, Sr`, `Open …` — **complete implementation + 29-vector regression set** in `dispatch-work\CODEX_V299_NAME_RETRY_REVIEW.md` (lines 17-117), refined rev at `CODEX_V2910_SHADOW_PARSER_REVIEW.md` (lines 41-144). Broadened `RE_NAMECOMMA` (compound surnames + particle window + suffix negative-lookahead) at CODEX_V299 line 129. Rev3 lesson (shipped v2.9.13): debris strips must become **BOUNDARY markers not spaces** + first-two-segments rule + anatomy stop-vocab, or reason tails enter names ("`<Name> Lumbar`") — the shadow program caught this in ONE live pull. Live end state: 69 distinct rows/pull, differs = 6 = OPEN slots only, zero real-name disagreements. Source: `EXT_V295_QUIET_PULL_HANDOFF.md` ~22:45.
- Known producer bypass to check: when a Patient/Name **table column** exists, the raw cell is accepted verbatim (`background.js:2719` and `:2053`) — no validation at all. Source: `CODEX_CLAUDE_FREEZE_HANDOFF.md` E1 follow-up table.

**1e. Chart-banner identity regex facts (why old readers failed / what fixed them):**
- Banner shape: `"<First MI LAST(ALL-CAPS)>"` newline `"NNyo M | MM-DD-YYYY | #<MRN>"`. Original v1.51 fail: name regex `\b([A-Z][a-z'\-]+,\s+[A-Z][a-z'\-]+)\b` can never match ALL-CAPS; DOB regex demanded a "DOB" label; cross-newline `\s+` produced the "Schaeffer,\n\nThe" phantom. Fixes: ALL-CAPS-tolerant tokens `[A-Z][A-Za-z'\-]+`, First-MI-LAST order, bare `MM-DD-YYYY` next to age/sex chip, `#8?\d+` MRN, **same-line-only matches**. Source: `live-verification\ISSUES.md` ISSUE 5.
- v1.64 pass-1 addition: when `looksName(lead)` fails on the encounter banner `"LAST, First (f) - …"` (trailing junk chips), validate a leading `"LAST, First"` **PREFIX**. Source: `WHAT_CHANGED_v164.md`.
- ENG-chip lookback: chip lines (language badge etc.) render BETWEEN the name and the demographics line — pass-1 looks back up to 3 lines skipping chip-like lines. Provider-credential rejection: final-token credential check kills "Schaeffer, MD" as a patient while "Do, John" passes. Hidden-frame demotion: frame body rect < 60px either way ⇒ score −6 (live: 0×0 phantom scored −6.8 vs real banner +6). Source: `HANDOFF_2026-07-09_EXTENSION_OPENER.md` §2h/§3.
- Shadow-DOM banner reader `mlsReadChartIdentityShadow` (v1.78): airlock/clientsummary banner lives in **open shadow roots** — `body.innerText` never contains it. Walk the flat tree of each shadow host, rebuild LINES, prefer the label block `First Name Used / Legal First Name / Middle Name / Legal Last Name / Date of birth / Patient ID` (values on the following line; prefer the *used* name — schedules carry it), else the chip line with name joined from the 1-3 lines above it (banner splits "Adam J" / "SCHAEFFER" across elements). Emit ONLY with both name AND DOB; fallback-only at gates. Source: `DAY_REPORT_2026-07-10_PERFECT_LOOP.md` §3, `EXTENSION_ARCHITECTURE.md` §7.

**1f. Grid-without-demographics contract (the certified fallback pattern for today's blocker):** when the grid renders no DOB, identity anchors on the row's own appointment context: open the chart FROM the exact row (or via appointmentId), read the banner name+DOB **in the opened chart**, freeze THAT as the proof ("identity via chart-text+DOB", day-pull v1.2.0 b95 lineage, live-proven; Michael explicitly authorized per-patient chart click as the DOB fallback in EXT_V149_DOB_REQUIREMENT.md item 3 — throttled, read-only, only for rows still DOB-less). Proposed contract already posted: `rowProof = {dob,mrn}` when rendered, ELSE `{appointmentId + banner-verified identity captured during the open}`. Source: `CLAUDE_CODEX_EXTENSION_SYNC_2026-07-14.md` 09:47 entry; memory `day-pull-v120-b95`.

## 2. CERTIFIED OPEN → CHART-READ → VISITS LOOP PARAMETERS

**2a. Opener — findpatient.esp route (schedule-INDEPENDENT; the 0/17→20/21 fix, v1.78–v1.94):**
- URL: `client/findpatient.esp?filtertype=NAME&findtext=last,first&CSRFPROTECT=<harvested from live frameset>` navigated in the main content frame. Bare navigation without findtext = server error page with no form. Search string: `lastname,firstname` **NO SPACE** (`Schaeffer,Adam`); normalize both "Last, First" and "First Last" inputs; nicknames "(Bob)" stripped for the search string; generational suffixes dropped ("Tom E Hatton, Jr" → `Hatton,Tom`); 3-token names: final token = surname, search first given token only; compound-surname retry ladder (v2.9.6): primary split → last-TWO-token surname → lastname-only + DOB row disambiguation.
- Re-fill the input via the **native value setter** (page init clears it) and re-check; click Find; **poll for Chart-link hydration** ("N results found" renders before row links exist); row match tiers: exact first → registered-nickname (word-boundary, "Robert (Bob)" matches roster "Bob") → prefix; **verify row DOB against the app store BEFORE opening**; 0 or >1 survivors after all tiers → honest refusal. dob-mismatch override (v1.94): exactly ONE exact-name row vetoed only by DOB → re-run once with no DOB and open it (read-only; every save gate still verifies the real banner) — stored DOBs can be junk (live case: stored 12/31/1940 vs athena 05/14/1990).
- **The Chart link is `javascript:redirectToAirlock(...)` — MUST be clicked from `world:'MAIN'`** (silently refused from the extension isolated world; "this single line was the difference between every open failing and every open working").
- Failure taxonomy (background.js 4427-4613 in v2.9.5): `no-name, no-content-frame, no-practice-prefix, findpatient-no-load, fill-not-sticking, no-find-button, blank-error, no-results, results-timeout, rows-not-rendered, dob-mismatch, no-name-match, ambiguous`. Ban the schedule scanner from ever scanning findpatient results (phantom "opens" + other patients' names).
- Receipts: 0/17 baseline → **20/21** same clinic day (v1.88, 2026-07-10); two same-name patients disambiguated by store-DOB; off-schedule + no-schedule patients opened. Sources: `DAY_REPORT_2026-07-10_PERFECT_LOOP.md` §2, `EXTENSION_ARCHITECTURE.md` §4, `CODEX_CLAUDE_FREEZE_HANDOFF.md` E2.

**2b. Schedule-row opener (when clicking the displayed grid):**
- Bare `el.click()` is DEAD on athenaOne v26.3 React rows. `realClick(el)` = dispatch `pointerover, mouseover, pointerdown, mousedown, pointerup, mouseup` then `.click()`. Do NOT include `'click'` in the pointer list (double-fires). Click the **smallest** matching descendant — the live-proven target is the row's `DIV.name` cell — never the huge row wrapper. Source: `HANDOFF_2026-07-09_EXTENSION_OPENER.md` §2b, `WHAT_CHANGED_v164.md`.
- BAD blocklist (`save|sign|order|bill|check-in|prescri|cancel|…`) must test only a CONTROL's own short label (a/button/[role=button], <34 chars) — testing the WHOLE ROW disqualified 42/52 afternoon rows via their own "Check-out"/"Cancelled" status text (root cause of every "afternoon pull failure"; morning runs worked because rows had no statuses yet). Source: `WHAT_CHANGED_v164.md`.
- Schedule-click can land on the **exam-prep/briefing** view (`/ax/appointment/<id>/briefing#qualityPane`): provider name renders, patient banner does NOT; a stale "REFRESH CHART / Go to Exam Prep" prompt sits on top. Fix: `mlsEnsureClinicalChartFn()` — detect briefing state, click only a safe load control (`REFRESH CHART`, else plain `Chart` link) via textContent scan (never innerText walk), then a **CHART-READY GATE** in `mlsAppChartRequest`: poll identity across frames (banner-preferred), nudge nav, wait for real name+DOB, **52s budget inside the app's 75s bridge timeout**, fail as `exam-prep-stuck`. The 13/13-era `_pullOne` used goHome → schedule-click → **wait 2600ms** → bare read — the 2600ms race against the briefing view is exactly what broke it. Sources: `HANDOFF_2026-07-09_EXTENSION_OPENER.md` §2a, `LIVE_TEST_v157_2026-07-08.md` regression diff.
- Never pass a patient name to `mlsAppReadChart` when the chart is already open (re-triggers openFn → blank mid-transition → false `athena-frozen-recovered`). Correct day-pull path: `mlsAppGoHome` → `mlsAppSearchOpenPatient({name})` → bare `mlsAppReadChart({patient:''})`. Source: `HANDOFF_2026-07-09_EXTENSION_OPENER.md` §2i.

**2c. Freeze guard / recovery cadence:**
- athenaOne renderer freezes after **~5–9 rapid chart opens/reads**. `__mlsReadsSinceReload` counts heavy reads; proactive reload-recovery **BEFORE the next open at counter=5** (v1.91; earlier go-home chunker fired at 6). `mlsRecoverAthenaTab(tabId)`: reload → wait complete → clear CSRF "unable to complete… **Continue**" interstitial (up to 3×; Continue not Cancel; session stays valid — never re-login) → reset counter → **re-arm keep-alive**. Retry-with-recovery: findpatient failures smelling like a freeze (injection timeout, findpatient-no-load, results-timeout, fill-not-sticking, no-content-frame, rows-not-rendered, blank-error) get one recovery + one retry of the SAME patient; a 90s visits-read hang also recovers. `mlsExecTO` timeout wrapper on every executeScript (a frozen renderer hangs it forever). Heavy all-frames `innerText` walks froze athena for 78s — never do them. Sources: `EXTENSION_ARCHITECTURE.md` §5/§13, `HANDOFF_2026-07-09_EXTENSION_OPENER.md` §8.
- Recovery call sites are exactly `background.js:2207,2294,2304,2964,3044,4664,4728,6233,7028,7041` (def at 1067, v2.9.5) — everything else has none. Bounded-retry doctrine: ONE retry only for proven read-only envelope failures (timeout/inject-rejection/zero-frames) with tab+host revalidation; NEVER retry identity/DOB/ambiguous/no-results/click/paste/write paths. Full insertion map: `CODEX_V299_NAME_RETRY_REVIEW.md` §C.

**2d. Visits pane reader (v1.89→v2.04, certified):**
- Rail item: `li.chart-tabs__list-item[data-chart-section-id="visits"]` — labels are CSS `::after` content (invisible to text scans). The rail LI is a **TOGGLE** (clicking twice closes). Drawer open-state is structural: `div[data-chart-section-id="history"]` x-position (~54 open, ~-420 collapsed) — heading-text checks lie (briefing/clientsummary cards render the same heading).
- **Settle-poll (v2.03):** heading renders before rows hydrate — poll ≤14s until rows appear OR an explicit zero signal ("All Events (0)" / "No visits…"); require **TWO consecutive equal row counts** before filing. ok-empty allowed ONLY with the explicit zero signal; declared N>0 with 0 parsed rows → retryable `rows-not-parsed`, never a silent empty (v2.01 honesty invariant). One Escape+re-click recovery for the clientsummary section-id/nav collision.
- **Foreground-for-read (v2.03):** a hidden tab NEVER hydrates the row list (page loop parked; live: "All Events (31)" with 0 rows for minutes, read fine once visible). v2.9.5 quiet-pull replacement: an **unfocused bounds update on a narrow right-edge work-strip window (~770px, focused:false) raises it above cover WITHOUT stealing focus** — occlusion = hidden on Win11/Chrome 150. Rows: 90s budget, one read at a time, normalized `{type,date,provider}`; **future-row rule (v2.04)**: any row dated after today = booking, skipped+counted, never history; identity-gated on the live DOM until match or 15s → refuse `wrong-chart/unverified/wrong-dob/wrong-id`. Sources: `EXTENSION_ARCHITECTURE.md` §8/§14, `DO_NOT_CHANGE_THE_EXTENSION.md`, `EXT_V295_QUIET_PULL_HANDOFF.md`.

**2e. Session/keep-alive:** blob-Worker `setInterval(postMessage, 55s)` driving a tiny synthetic mousemove + 1px scroll (never main-thread setInterval — throttled to ~0 hidden); dies on every reload — extension re-arms it after every recovery and on each athena content-script hello (8s dedupe). Two-window trap: unified picker = heartbeat registration + read-only reachability ping (`.calendar-nav`/frameset promotes signed-in shell) + login hosts excluded + lastAccessed tiebreak + 10s cache; a stale tab's content script pings `mlsPong` with an EMPTY version (the tell). Sources: `EXTENSION_ARCHITECTURE.md` §3/§10, `HANDOFF_2026-07-09_EXTENSION_OPENER.md` §8.

**2f. Current-gen (07-14) briefing-surface visits loop — the r4 two-stage reader (proven cold 31/31 across 5 patients):** briefing frame has `ul.encounter-list.autostart` with `li.encounter-list-item` rows (mixture: vitals/future-appointment/patient-case/previous-visit; **only canonical previous-visit rows count in the denominator**); rows carry `data-encounter-id/data-provider-id/data-section/data-sections`; clicking the LI is a **no-op** — click descendant `div.clickable.accordion-trigger` (opens metadata), then `span.slideout-trigger-open` (opens a NEW encounter-summary child **iframe** with the clinical detail); require the frame's URL to contain the row's frozen `data-encounter-id` (chart-open PRE-CACHES an iframe of the most recent encounter, defeating content-delta proofs); wait for `open===true AND iframeContract===true` (slideout container flips open in ~100-200ms, iframe arrives seconds later on a cold chart — cold ceiling 8-10s); close-verify (descendant close controls + Escape fallback + poll frame-set gone) before the next row or every later row fails `stale-encounter-frame-open`; `bodyMinimal:true` contract for URL-bound short bodies. Source: `CLAUDE_CODEX_EXTENSION_SYNC_2026-07-14.md` 18:29–19:58 entries.

## 3. LIVE-VERIFIED RECEIPTS TIMELINE (what worked when)

| Date | Build | Result | Source |
|---|---|---|---|
| 07-07 | v1.51 | Schedule 38 rows / 30 DOB structured; app discarded appts array (text parse) | live-verification\ISSUES.md |
| 07-08 | v1.55 + b86 `__mlsDayHistoryPull` | **13/13** day-history (goHome→read→verify→ingest, 8-15KB charts, CPT/ICD, DOB, persisted) | MASTER_STATUS_2026-07-08.md §7, DAY_PULL_STATUS_2026-07-08.md |
| 07-08/09 | v1.57 | 0/23 — NOT a code regression (pull path byte-identical to 13/13); athena schedule-click landed on briefing; afternoon status-text blocklist (found later, v1.64) | LIVE_TEST_v157_2026-07-08.md |
| 07-09 | v1.62-1.64 | realClick + REFRESH-CHART nav proven live (17.7KB chart, correct DOB via banner) | WHAT_CHANGED_v164.md |
| 07-10 AM | v1.77→v1.88 | 0/17 (wrong-department schedule displayed) → **20/21** via findpatient opener; shadow-banner identity `patientStatus:"match"` on test record | DAY_REPORT_2026-07-10_PERFECT_LOOP.md |
| 07-10 night | v1.91-1.98 | Certification: 23/23 DOB+MRN+clean history+visits; double-pull 0 dupes at 1137 patients; month 30/30 days 856 found/0 failed; name-shape trio 5/5 | CERTIFICATION_REPORT_2026-07-10_NIGHT.md |
| 07-11 | v2.03/v2.04 | 6/6 first-try backfill visits reads; all-future panes honest-empty; write→verify→delete on #7833832 (b129) | DO_NOT_CHANGE_THE_EXTENSION.md, EXTENSION_ARCHITECTURE.md §15 |
| 07-13 | v2.9.5-2.9.14 | Quiet pull 10/13 zero-yank 5.5min; dedup 50→25; shadow parser 69 rows/pull differs=OPEN-only; month stress 69 charts ~50min | EXT_V295_QUIET_PULL_HANDOFF.md |
| 07-14 | v2.9.21/22 | Schedule **18/18 DOB 18/18** (legacy filled-appointment-row lane); history 0/18 → r4 slideout reader → cold **31/31** bodies 5 patients; managed day wedged on app-side sync archive scan (perf fix `afee93b`) then 18 schedule / 0 history (deadline gaps) | FABLE_EXTENSION_HANDOFF_2026-07-14.md, CLAUDE_CODEX_EXTENSION_SYNC_2026-07-14.md |
| 07-15 | v2.9.24 | Wednesday: schedule 17/17 PASS but DOB/MRN **0/17** (grid renders none — raw capture proof) + history 0/17 `patient-not-resolved` (fresh-create path: `feat_mls_schedimport_exact.js:975` queueHistory fires before the created patient's local id exists; Tuesday masked it because all patients already existed → enrich path) | CLAUDE_CODEX_EXTENSION_SYNC_2026-07-14.md 09:27-09:47 |

## 4. ATHENA DOM SHAPES / GOTCHAS INDEX

- `javascript:` hrefs need `world:'MAIN'`; chart rail labels are `::after`; freeze after ~5-9 chart interactions; CSRF Continue interstitial after reloads (never re-login); week strip `.calendar-nav` — TODAY tab is label-less, arrows disambiguated only by containers `.nav-prev-week`/`.nav-next-week`, weekend targets live in NEXT week's strip; dashboard schedule widget = signed-in provider only; History note editors are contentEditable DIVs (some `plaintext-only`) — only real key events + a real blur persist; `contains()` is false across shadow boundaries (climb `parentElement→host`); UTA affordances `.uta-note-label-add-note .clickable` / `span.uta-note-label-note-text` (click the INNERMOST text element). Full index: `EXTENSION_ARCHITECTURE.md` §13-15.
- `Calendar → View All Appointments` (`schedule/viewdepartment.esp?DATE=…&DEPARTMENTID=…`) lists ALL providers as columns, cells like `EP10 - PEARSON, ROBERT R (77yo M) #7697536` (see `b121-pack\feat_mls_b121_pack.js:3093`) — but cells are plain `<td>`s with no click handler: enumerate-only. Source: `MORNING_REPORT_2026-07-10.md` §4.
- Global search: synthetic keystrokes DON'T work; REAL keystrokes do (`last,first` + Enter → Find-a-Patient "1 result found" with Quickview/Chart/Case/Schedule actions). v1.51's search-box filter excluded inputs whose label matched `\bid\b` — athena's placeholder "Find patient, claim, or claim ID" self-disqualified. Sources: `MORNING_REPORT_2026-07-10.md` side-discovery, `live-verification\ISSUES.md` v1.52 list.
- Day grid VIRTUALIZES on the modern variant (vertical + horizontal) — single scrape captures only rendered chips; multi-scroll re-pulls recover most; grid also truncates PR-chip names. Source: `mls-full-site-backend-qa-fix\task-2-backend-calendar-appointments\qa-followup\QA_FOLLOWUP_REPORT.md`.
- Hidden/occluded window = fully throttled (rAF 0/s, timers 1Hz→1/min); page-level moveTo of a background popup is ignored; unfocused bounds-update raises without focus (the quiet-pull trick). Source: `EXT_V295_QUIET_PULL_HANDOFF.md`.
- v2.9.24 session-health: `mlsAthIsLoginish` on top-level URL alone is fail-open — a nested login iframe shows "Refresh Timed Out / session timed out" while the outer frameset looks fine; all-frame probe required. Source: sync file 00:27-00:56 entries.
- Stale hidden chart frames return a previous patient's banner on a bare read — never trust a banner without cross-checking the expected patient + hidden-frame demotion. Source: `HANDOFF_2026-07-09_EXTENSION_OPENER.md` §2h.

## 5. PATIENT-CREATE / IMPORT FLOW (fresh day WITH history)

- App import chokepoint: `window._importPulledSchedule(appts)` — upserts patient, dedupes via `_apptKey` = `patient_external_id|name|dob|date|time` shape (see `b114\mls-connect.b114.js:12053,31940-31960`), persists via `upsertPatient` keyed **name+DOB** (F18), posts `{name, dob, reason, patient_external_id, appt_date, start_at, provider}` to `/api/appointments`. OPEN rows and provider-credential rows are stripped at this gate. Beware the historic wrapper-cycle killer: multiple modules re-wrap `_importPulledSchedule` on timers — stand-down markers `__b49/__provWrap/__prf` must be stamped synchronously or the chain becomes a CYCLE (b64 chainfix; schedule imports were silently dead for days). Source: `live-verification\ISSUES.md` ROUND 2 + b114 bundle.
- DOB backfill (b66 chainfix v2.3): import-time enrichment alone can't heal existing rows because the base pull dedupes upstream and never calls the import fn for existing rows — a passive backfill on every structured schedule reply (fill-only, normalized-name match, never overwrite, skip conflicts) is what actually landed DOBs (16 appt + 13 patient rows, 0 conflicts, spot-checked exact vs the Athena screen). Also standing `__mlsDobSync` roster backfill (482 rows, 2026-07-06). Sources: `live-verification\ISSUES.md` b66, `EXT_V149_DOB_REQUIREMENT.md`.
- Ledger: si-1.x schedule-import ledger keys on **name+DOB** — a re-pull after a name fix ADDS a corrected row beside the stale one (never re-pull to "fix" a bad row; owner deletes). Source: `EXT_V295_QUIET_PULL_HANDOFF.md` DATA LEFTOVER note.
- The 07-14 successful fresh-import receipt shape: "calendar attempted 17/accounted 17/**created 17**/repaired 0/failed 0" (Wednesday) vs "added 1 / enriched 17" (Tuesday). The fresh-CREATE bug: `feat_mls_schedimport_exact.js:975` — `queueHistory(a,p,date)` pushes `patient-not-resolved` when `p.id` is empty; the overnight hardening binds appointments to an immutable local patient id BEFORE async work, but on create the local record materializes only after server create + refresh. Fix direction (already pinned): resolve the created patient id from the create response / post-create refresh BEFORE queueHistory, keeping immutable-id + row-proof semantics. Second coupled fix: with ids present, all 17 would then fail `missing-source-dob-mrn-proof` — needs the §1f appointmentId+banner proof lane. Source: `CLAUDE_CODEX_EXTENSION_SYNC_2026-07-14.md` 09:27/09:34.
- History-run reason-collapse trap: `mls-connect.js:9048/9056/9066` collapse precise extension reasons to `gohome-failed/open-failed/read-failed` — this hid real causes for days; preserve `findReason/error` (the b121 pack already does at `feat_mls_b121_pack.js:2829-2832,2917`). Source: `CODEX_CLAUDE_FREEZE_HANDOFF.md` E3.
- Month leg requires the month's schedule imported first (roster matcher keys on provider+month rows); per-patient machinery identical to the proven day loop. Source: `DAY_REPORT_2026-07-10_PERFECT_LOOP.md` §5.2.

## Key reference source trees (older proven code, for porting not reverting)
- v1.55 (13/13 baseline): `dispatch-work\extension-v155-prep\src\` · v1.57 twin: `dispatch-work\overnight-bugfixes\ext-v157-src\` · v1.62-1.6x/v2.x lineage: `dispatch-work\extension-v159-prep\src\` + `WHAT_CHANGED_v159..v166_v167.md` · proven visits-r4 accordion reader commit `6cd1a43` (preferred `li.encounter-list-item.accordion-open` as the encounter-scoped body) · v2.9.21 legacy-grid commit `bd37e9e`.

---

All mining complete. Here is the structured brief.

---

# PROVEN TECHNIQUES BRIEF — Athena day-grid demographics + certified open/read loop (PHI-free)

## A. LINEAGE MAP (which extractor earned which receipt)

| Receipt | Version | Where the code lives |
|---|---|---|
| 13/13 history pull baseline | v1.55 (=v1.57 pull path, byte-identical) | `C:\Users\Micha\Desktop\MLS_EVERYTHING\dispatch-work\extension-v155-prep\src\background.js`, `...\overnight-bugfixes\ext-v157-src\background.js` |
| 44/52 schedule DOB capture (rest = OPEN slots) | v1.61/1.62 era, but the DOB code is v1.51's `mlsAttachDobs` (unchanged since) | v157 `background.js:374-415`; still present in current lineage (`ui-unification-20260714\background.js:2924-2960`) |
| Day pull 0/17 → 20/21 | v1.88 findpatient.esp opener + shadow identity reader | `...\dispatch-work\extension-v159-prep\src\background.js` |
| 18/18 patients + **DOB 18/18** on the legacy day grid, 2026-07-14 | v2.9.21 legacy day-grid strategy | `...\dispatch-work\ui-unification-20260714\background.js:5422-5496`; receipts in `...\release-b273-integration\FABLE_EXTENSION_HANDOFF_2026-07-14.md` |

**Critical insight for the 0/17 DOB blocker:** No proven version EVER extracted DOB from per-row DOM attributes. The v2.9.21 legacy-grid DOM lane extracts only `{time,name,provider,appointmentId,providerId,reason}`; **DOB has always come from a separate worker-side TEXT pass (`mlsAttachDobs`) over the schedule frame's `innerText`**, applied to the merged appts at the message-handler level (v157 `background.js:1525`: `appts: mlsAttachDobs(__mlsM.appts, pick.t)`). Athena's day-grid row text carries DOB inline in the shape `<Name> 61yo M | 06-17-1965` (documented live, `HANDOFF_2026-07-09_EXTENSION_OPENER.md` §5). A "strict extractor" that demands labeled `DOB:` fields or `data-patient-dob` attributes on grid rows will get 0/N because those don't exist on the day grid — the strict anchored extractor at `ui-unification-20260714\background.js:350-391` (requires exactly 1 name + 1 dob + 1 mrn inside an explicit patient-header container) was designed for the CHART banner, not schedule rows.

## B. EXACT DOB/NAME EXTRACTION CODE (the pieces that hit 18/18 and 44/52)

**1. `mlsAttachDobs(appts, text)` — the DOB attacher (v1.51, live-proven, unchanged through v2.9.21).** Source: v157 `background.js:391-415`.
- Split frame text into lines (cap 4000). For each appt lacking `dob`: tokenize name to lowercase words len>1; find a line containing the LAST name token (and the first, if 2+ tokens); search `lines[i] + ' ' + lines[i+1]` ("athena wraps DOB under the name") with `DATE_RE = /\b([01]?\d[\/\-\.][0-3]?\d[\/\-\.]\d{4})\b/g` (4-digit year only); accept first date passing `mlsPlausibleDob`.
- `mlsPlausibleDob` (v157 `background.js:379-390`): `^([01]?\d)[\/\-\.]([0-3]?\d)[\/\-\.](\d{4})$`, year ≥1900 and ≤ current year, **rejects a date equal to TODAY** ("schedule date bleeding through"), normalizes to `MM/DD/YYYY`. "Conservative: no match → no dob (never guessed)."

**2. v2.9.21 legacy day-grid row reader (the 18/18 schedule reader).** Source: `ui-unification-20260714\background.js:5430-5496` (inline injected `mlsSchedDomInline`), mirrored in `...\inject_dom.js`.
- Containers: `[class~="appointments-container"]` (one `<ul>` per scoped provider list; **the live dashboard renders the same list TWICE for alternate sort modes**).
- Rows: `[class~="filled-appointment-row"]` inside each container. **No text-length ceiling** (v2.9.20 failed 17/38 because the generic fallback discarded a >300-char row).
- Provider header: `[class~="appointment-header2"]` inside the container, validated by `looksLikeProviderHeader` (credential regex `RE_CRED` + name-comma regex).
- Slot filter: strip times (`RTG=/\b\d{1,2}:\d{2}\s*(?:[ap]\.?\s?m\.?)?\b/gi`) then `/^(?:(?:\d+\s*(?:min(?:ute)?s?|mins?)\s*)?)(?:open|blocked?|hold|unavailable|lunch|closed|administrative|admin|reserved)(?:\b|\s)/i`.
- IDs from attributes/href: `data-appointment-id`, `data-appt-id`, `data-appointmentid`, `appointmentid`, `data-provider-id`, `data-rendering-provider-id`, `data-resource-id`, plus href regexes `[?&#/](?:appointmentid|appointment_id|appointment|apptid)[=/:]([a-z0-9_-]{2,})`.
- Dedup: by appointmentId first, else `time|normalized-name|providerKey`; hidden providerless twin dropped when an attributed copy exists; unresolved rows (no time or <2 name tokens) stay in the completeness denominator so partial reads **fail closed**.
- Live receipt: 40 rendered rows → 36 patient observations (2×18) + 4 OPEN → exactly 18 patients; import receipt add 1 / enrich 17; DOB 18/18 (handoff §1, §3).
- Frame exclusion (v2.9.8, live catch — staff-message thread parsed as a fabricated appointment): skip frames whose pathname matches `/stm\.esp|\/coordinator\/|messaging|letters/i` (`background.js:5415-5421`).

**3. Patient-name-from-row parser (all versions).** v157 `background.js:1026-1040`: prefer `RE_NAMECOMMA=/([A-Z][A-Za-z'’-]+)\s*,\s*([A-Z][A-Za-z'’-]+)/` on the row text; else strip times and collect up to 3 leading capitalized words, rejecting the STOP list (visit-type/status words) and credential tokens. v2.9.20 note (`background.js:5506-5518`): the structure-lane `_nmS` that captured only text after the comma left one-token names on virtualized rows → silently rejected; fixed by canonical `mlsParseName` first, legacy `,\s*([A-Z]...)` fallback second.

**4. Modern/React grid variant (day grid when Athena serves the React shape):** appts are `[class*="PatientAppointment_appointment-container"]` buttons inside `[class*="ScheduleColumn_schedule-column"]` sections; provider assigned by column x-center vs header x-center; **grid virtualizes BOTH axes** — sweep a bounded Cartesian scroll product (h-step `clientWidth*0.55` min 160px, v-step `clientHeight*0.65` min 120px, ≤24 steps/axis, 15-30s budget, 90ms settle + retry), key rows by `time|name|provider` NOT bare DOM id ("React recycles appointment nodes"), and restore exact scroll state (`inject_dom.js:30-57`). Detection selector union: `'[class*="PatientAppointment_appointment-container"], [class*="ScheduleColumn_schedule-column"], [data-testid*="schedule-grid" i], [aria-label*="schedule grid" i], [class~="appointments-container"], [class~="filled-appointment-row"]'`.

**5. Schedule frame picking (v1.55/v1.57, content-scored not URL-bound)** — v157 `background.js:1506-1523`: read ALL frames' innerText; score = clock-time count ×2 (capped 60) + keywords (`appointment, schedul, provider, arrived, checked in, exam room, no show, walk-in` +6 each) + URL hint +25 + day/month words +2 − messaging words −20. Date header: `h1.fe_c_heading--subsection` → `schedDate` (background.js:1420,1434).

## C. CERTIFIED OPEN → CHART-READ → VISITS LOOP

**1. Visits reader orchestration (the 13/13 engine).** v157 `background.js:2377-2731`:
- `ORCH_DEFAULT = { maxVisits: 60, waitMs: 1400, initialWaitMs: 1000 }` (cfg overridable via `chrome.storage.local.mlsAthenaVisitsCfg`).
- **v1.54 THROTTLING FIX (live-verified):** foreground the Athena tab before reading — "foreground athenaOne read 38 visits; the same read timed out in the background" (background tab DOM reads ~9x slower). `chrome.tabs.update(emrId,{active:true})` + `chrome.windows.update(...,{focused:true})`; restore MLS tab focus after (`:2656`, `:2730`).
- Pipeline: sleep(initialWaitMs) → inject `mlsVisitsDriverFn('identity')` all frames, pick best by `(name?2)+(dob?1)` → `'diagnose'` (PHI-free structural map saved to `mlsAthenaVisitsDiag`) → `'enumerate'` (candidate-group scoring) → if ≥60% rows "rich" (len≥minRealLen && (CPT/ICD || len≥220)) read in place (`'readExpanded'`, zero clicks); else per-row `'click'` → sleep(waitMs=1400) → `'detail'` across frames, keep best by raw length.
- Row selectors: `'tr','[role="row"]','li','.encounter','.encounter-row','[data-encounter-id]','[id*="encounter" i]','[class*="encounter" i]','.visit','[class*="visit" i]','[class*="timeline" i] li',...`; groups formed per-parent, must have ≥2 dated rows; group score = Σ rowScore + count×2 + type/click bonuses. Detail selectors: `'[role="main"]','main','.encounter-detail','.documentation','article','[class*="encounterbody" i]','[class*="notebody" i]',...`. `DATE_RE=/(?:^|[^\d])(\d{1,2}[\/\-.]\d{1,2}[\/\-.]\d{2,4}|\d{4}-\d{1,2}-\d{1,2})(?!\d)/`, `CPT_RE=/\b\d{5}\b/g`, `ICD_RE=/\b[A-TV-Z]\d[0-9A-Z](?:\.[0-9A-Z]{1,4})?\b/g`. Click excludes: save/sign/finalize/post/bill/submit/delete/lock/addend/amend/discard/cancel appointment/close encounter/checkout.

**2. Day-pull per-patient sequence (proven 13/13 then 20/21):** `mlsAppGoHome` → `mlsAppSearchOpenPatient({name})` → wait ~2600ms (app side) → **bare** `mlsAppReadChart({patient:''})`. Never pass a patient name to `mlsAppReadChart` when a chart is already open (re-triggers navigation → blank mid-transition → false `athena-frozen-recovered`). Source: `LIVE_TEST_v157_2026-07-08.md` §regression, `HANDOFF_2026-07-09` §2i.

**3. CHART-READY GATE (v1.59-v1.86, live-proven).** `extension-v159-prep\src\background.js:2741-2840`:
- `BUDGET_MS = 52000` (inside app's 75s bridge timeout); identity injection timeout 20s (15s caused false frozen-recover); 2 consecutive injection timeouts → `mlsRecoverAthenaTab` + honest `athena-frozen-recovered`.
- Poll `mlsReadChartIdentity` all frames each round; expected-patient match via ≥2 shared name tokens (`nmm`); shadow-DOM fallback (`mlsShadowIdentityTry`) only from poll≥2 or after a nav click, or when classic candidate is a weak non-banner grep ≠ expected (care-team names live in chart light DOM).
- If briefing detected: inject `mlsEnsureClinicalChartFn` (10s TO) which clicks ONLY `REFRESH CHART` (preferred) or a bare `Chart`/`Full Chart`/`Patient Chart` link, guarded by `BAD=/save|sign|order|delete|discard|remove|void|submit|bill|charge|check\s*-?\s*(in|out)|prescri|refill|dispense|cancel|log\s*out|apptmnt|reschedul/i`, scanning ≤900 controls via `textContent` (NEVER innerText — the all-frames innerText walk caused the documented 78s freeze). After click: foreground the tab for the load, sleep 3200ms, keep polling. Sleep between rounds 2400ms. Briefing + 5 no-click rounds → `exam-prep-stuck` (honest fail). Accept weaker identity only after 42s and only score ≥0 (junk-frame guard); **never return a negative-score identity**.
- Click technique (`realClick`, the v1.62 fix — bare `.click()` is DEAD on Athena's React `<div>` rows): dispatch `pointerover, mouseover, pointerdown, mousedown, pointerup, mouseup` with real coordinates, then ONE `.click()` (never include `'click'` in the dispatch list — double-fires). Click the SMALLEST element matching the name, not the first/wrapper.

**4. Freeze-guard cadence:** Athena freezes after ~5-9 consecutive chart opens+reads. v1.91: `if (__mlsReadsSinceReload >= 5)` reload-recover BEFORE the next open (`:4464-4467`); older go-home chunker used ≥6. `mlsRecoverAthenaTab` (`:867-884`): `chrome.tabs.reload` → wait `status==='complete'` (20s cap) → sleep 1500ms → up to 3× click the CSRF interstitial's exact-text `Continue` button (`mlsAthenaContinueFn`, gated on `/unable to complete|could not complete|session (has )?(expired|timed)/i` body text) with 2200ms between → reset counter → **re-arm keep-alive** (reload kills it). Keep-alive: page-context (world MAIN) Web-Worker 55s tick → tiny synthetic mousemove + 1px scrollBy, no DOM scans (`:893+`).

**5. findpatient.esp opener (v1.88, schedule-INDEPENDENT, took 0/17→20/21).** `extension-v159-prep\src\background.js:4212-4416`, `mlsFindPatientOpenDriverFn`:
- Runs in the TOP frame (survives content-frame navigations) but the executeScript **MUST use `world:'MAIN'`** — Athena's `javascript:` hrefs (redirectToAirlock) silently refuse to navigate from the ISOLATED world (v1.83, "the big one").
- Content frame = deepest same-origin frame with element area >150000, skipping `/globalnav|statusbar|stm\.esp|schedulenavclose|coordinator\/enterprise|blank\.html/i`.
- Harvest practice prefix `/(\d+)/(\d+)/` and `CSRFPROTECT` token from frame URLs; navigate to `client/findpatient.esp?filtertype=NAME&findtext=<last,first>&defaultaction=<prefix>client/clientsummary.esp&CSRFPROTECT=<tok>`. Bare findpatient.esp without findtext = server ERROR page with no form.
- Search string shape: **`lastname,firstname` NO SPACE**; strip `(Nickname)` parens and generational suffixes (`jr|sr|ii|iii|iv|v|esq`) for search only.
- Fill via native value setter (page init CLEARS the input — settle 900ms, re-query node each try, verify value stuck, up to 4 tries); click exact-text `Find`; poll body for `/\d+\s+results?\s+found|no results|cannot leave the find text field blank/i` (12s).
- **Hydration gotcha:** "N results found" text renders BEFORE row Chart links hydrate — poll up to 6s for `<a>` with exact text `chart` before scanning rows.
- Row match: two-tier (exact first-name cell match incl. word-boundary nickname hit inside "Legal (Nick)" cells → else prefix); **verify row DOB against the wanted DOB before clicking** (row DOB regex `([01]?\d)\/([0-3]?\d)\/(\d{4})` over `<td>` cells); report `dob-mismatch` instead of silently dropping (stored roster DOB can be junk). Single candidate only; `ambiguous` otherwise.
- **Return IMMEDIATELY after the Chart click** — the app's open bridge budget is ~18s; the READ's 52s chart-ready gate is the designated waiter (v1.82: a settle loop here blew every open).
- Never let the schedule scanner scan the findpatient RESULTS page (phantom opens; banned by pathname v1.84; also `:2857` drops findpatient.esp frames from identity reads).

**6. Chart-banner identity reader (v1.52 rewrite, live-proven).** v157 `background.js:292-371` (`mlsReadChartIdentity`):
- Banner line shape: `Name / 20yo M | 03-24-2006 | #7833832`. Regexes: `AGE_CHIP=/\b(\d{1,3})\s*(?:yo|y\/o|yrs?\.?|years?\s*old)\b/i`, `BARE_DATE=/\b([01]?\d)[\/\-\.]([0-3]?\d)[\/\-\.](\d{4})\b/`, `MRN_HASH=/#\s?(\d{4,})/`. Name = text before `[·|]` or first digit on the chip line, or the line above; LINE-BY-LINE matching only (cross-newline matching produced garbage); ALL-CAPS accepted; STOP-word veto. Later ENG-chip fix: look back up to 3 lines skipping chip-like lines (banner splits first/last name across lines).
- Scoring: dob+2, mrn+2, name+1, banner-via+3, chart-route URL +2, `stm.esp|globalnav|statusbar|inbox|messag` −4, "unread messages" −4; **hidden-frame demotion**: body rect <60px either dimension ⇒ −6 (live: a 0×0 phantom scored −6.8 vs real banner +6). **`if (!name) { dob=''; mrn=''; }`** — never emit DOB/MRN without a verified name in the same frame.
- Shadow reader (`mlsReadChartIdentityShadow`, v1.78+): flat-tree walker over open shadow roots; parses labeled drawer block — `First Name Used` (prefer over `Legal First Name`)/`Middle Name`/`Legal Last Name`/`Date of birth`/`Patient ID`, values on the FOLLOWING line — else chip line + name joined from 1-3 lines above. Emits only with BOTH name+DOB.
- Stale hidden chart frames return real-looking banners for the WRONG patient — never trust a banner without cross-checking the expected patient (`HANDOFF_2026-07-09` §2h).

## D. PATIENT-CREATE / IMPORT-WITH-HISTORY FLOW (and where 0-DOB kills it)

Source: `ui-unification-20260714\feat_mls_schedimport_exact.js` (si-1.5.1; same file in `live-record-voice-b265`, `live-tooltip-sweep-20260714`):
- Patient identity ladder (`patientIdentity`, :216-228): bound local id (with proof) → `athena-patient:<sourceId>` → `mrn:<mrn>` → `nd:<normName>|<normDob>` → `''`. **A display name alone is never an identity** (`findPatient` :306-350 returns null on name-only).
- Fresh create (:639-643): only when `patientKey` is non-empty (i.e., row has DOB or MRN) → `upsertPatient({ id: stableId(patientKey), ... })` where `stableId` = FNV-1a hash → `"p_sched_" + hash.toString(36)`; sets `a.patient_external_id = np.id` immediately.
- History queueing (`queueHistory` :540-572): requires resolved `patientId` + DOB/MRN proof ON THE ROW (or on an already-bound backend appointment for that exact patient); pushes `{patient_external_id, _mlsTargetPatientId, _mlsTargetDob, _mlsTargetMrn, name, dob, mrn, athenaId, date, source:'athena-schedule-history'}`; otherwise `historyUnresolved` with reason `patient-not-resolved` / `missing-source-dob-mrn-proof` / `source-proof-conflict`.
- **Direct implication for the current blocker:** if the schedule extractor returns DOB/MRN for 0 rows, `patientIdentity` is `''` for every fresh row → no local patient is created, no `patient_external_id` resolves, and every history queue entry is unresolved. The fix is restoring row-level DOB (the `mlsAttachDobs` text pass over the day-grid frame text, or per-row inline-text DOB parse), not loosening identity.
- DOM-scrape fallback keeps extension-supplied `dob` (FIX 2026-07-01, :370-385 — it was previously mapped to `""`); provider carried into POST body (provider-null rows broke doctor-scoped views); dedup ledger `p_sched_…` in account-namespaced localStorage (`schedImportIndexV1`), pending TTL 5min, rollback on failed POST; date resolution: per-row parsed date → page-printed date (`_detectSchedDate`) → requested day, with requested day outranking a week-banner pageDate; selected-provider imports fail closed on any unattributed row.

## E. HISTORY-LANE DOM TRUTH (current v2.9.22 findings, for the chart side)

From memory `ext-v2922-history-lane-and-eol-landmine.md`: on the briefing/exam-prep visits list, 16 rows = 1 vitals / 2 future / 4 case / 9 previous-visit; a bare `li` click is a NO-OP; the **accordion-trigger yields metadata only**; the **slideout-trigger opens a child iframe containing the real visit body** — that iframe is the read target. Current P0 failure classes on the Wednesday run (`FABLE_EXTENSION_HANDOFF_2026-07-14.md` §5): opens land on `/ax/appointment/<id>/briefing` with no patient banner (`exam-prep-stuck`) and `same-frame-name-mismatch` — both correct refusals; the fix is the proven `realClick` + `REFRESH CHART`/`Chart` nav + banner-wait sequence above, serialized per patient, no-yank preserved.

## F. KEY GOTCHA INDEX (one-liners)

- DOB source = frame TEXT next to the name, never labeled fields, on the day grid (`Name 61yo M | MM-DD-YYYY` inline).
- `DATE_RE` for schedule DOB requires a 4-digit year; DOB==today is rejected.
- Bare `.click()` dead on React rows → pointer/mouse prelude + one click.
- `javascript:` hrefs need `world:'MAIN'`.
- `lastname,firstname` no space for Athena search; strip nicknames/suffixes.
- Background tab = ~9x throttled DOM; foreground for reads, restore focus after.
- Freeze after ~5-9 reads → proactive reload + `Continue` interstitial + re-arm 55s keep-alive.
- "N results found" renders before Chart links hydrate — poll links, not text.
- Hidden/stale frames carry phantom identities — demote <60px frames, never emit DOB without same-frame name, never accept negative-score identity.
- Legacy grid duplicates its whole `<ul>` per sort mode — dedupe by appointmentId else time|name|provider; OPEN/capacity rows are slots, not patients.
- Opener returns immediately after click; the 52s read gate is the waiter (18s open budget, 75s bridge).
- Never scan findpatient results or `stm.esp`/coordinator/letters frames for schedule/identity data.

---

I have everything needed. Compiling the brief.

# MINING BRIEF — proven, live-verified Athena day-grid + history-loop techniques

## A. THE SMOKING GUN for the 0/17 DOB regression (highest value)

**A1. Day-grid DOB is a BARE, UNLABELED date in row text — the strict extractor can never see it.**
- Live row shape captured 2026-07-09 (`WHAT_CHANGED_v164.md`): `"3:20 PM 20min <First Last> 51yo F | 07-16-1974 Lumbar Spine Check-out …"` — DOB renders after the age/sex chip with **no "DOB:" label, no data-* attribute**.
- The current worktree's strict extractor (`C:\Users\Micha\Desktop\MLS_EVERYTHING\dispatch-work\release-b273-integration\background.js` — `explicitRowIdentity` line 4490 and its inline twin `_scheduleRowProofD` line 5830) accepts DOB/MRN ONLY from: `data-patient-dob/data-dob/data-date-of-birth/data-birth-date` attrs, `aria-label`/text with an explicit `\b(?:DOB|Date of Birth|Birth Date)\s*[:#-]?` label, or `data-testid` matching `(?:dob|date[-_]?of[-_]?birth|birth[-_]?date)$`. Comment says "A bare date... is never a DOB." → 0/17 on a grid that only ever shows bare dates.
- **The old working technique** (v1.51, live 13/13→23/23→18/18): scraper returns `{time,name,provider}`; then `mlsAttachDobs(appts, frameText)` scans the frame TEXT: for each appt lacking dob, find a line containing the patient's LAST name (and FIRST if ≥2 tokens), scan that line **plus the immediate next line** ("athena wraps DOB under the name") with `DATE_RE=/\b([01]?\d[\/\-\.][0-3]?\d[\/\-\.]\d{4})\b/g`, validate via `mlsPlausibleDob` (4-digit year ≥1900, not future, **NOT equal to today** — schedule-date-bleed guard), emit `MM/DD/YYYY`, never guessed. Exact code: `dispatch-work\extension-v159-prep\src\background.js` lines 521–562; **still present verbatim in the shipped v2.9.21 zip** (`release-b273-integration\MLS_Assist_v2.9.21.zip` → background.js line 2941) and wired at response time: `appts: mlsAttachDobs(__mlsM.appts, (pick&&pick.t)||'')`.
- **The regression**: the current worktree background.js line 2988 rewrote `mlsAttachDobs` into a validate-only pass (`a.dob = mlsPlausibleDob(a.dob)`) that **never scans text** — DOB now must come from `_scheduleRowProofD`, hence 0/17. v2.9.21 (same practice, same legacy grid, 2026-07-14) got **DOB 18/18 live** with the text-scan version (`release-b273-integration\FABLE_EXTENSION_HANDOFF_2026-07-14.md` §1, §3). Restoring the v1.51 text-scan as a fallback tier after the strict row-proof (strict wins on conflict) reconciles both designs.

**A2. Fresh-create/local-id blocker is coupled to A1.** `release-b273-integration\feat_mls_schedimport_exact.js` (si-1.6.2): `patientIdentity(a)` returns `"local:"+localId` only when `proof.dob || proof.mrn` (or allowBoundLocal); history queueing pushes `{reason:"missing-source-dob-mrn-proof"}` / `{reason:"patient-not-resolved"}` and **removes** the history target (lines 388–430, 944–1021). With DOB 0/17 no row binds and nothing queues. The old proven import path (app bundle `dispatch-work\b114\mls-connect.b117.js` lines 7440–7540 + F18b at 15116): late `_importPulledSchedule` wrapper drops OPEN/BLOCK/HOLD/LUNCH placeholders + provider-credential rows, `expandTruncatedBatch` (full names), `enrichDobsFromStructured` (DOB by normalized-name match from the passive `mlsAppScheduleResult` stash — fill MISSING only, never overwrite, skip conflicting names), dedups on **name+DOB**, persists via `savePatients`/`upsertPatient`; visits persisted by calling `addVisit` DIRECTLY (b95 fix — `ingestChart`'s batch path silently loses visits; `DAY_PULL_STATUS_2026-07-08.md`).

## B. Exact schedule-grid selectors/regexes that worked live

**B1. Legacy day grid (the current practice's live shape, 18/18 on 2026-07-14):**
- Containers: `[class~="appointments-container"]` (one `<ul>` per scoped provider list; **the dashboard renders the day list TWICE** — sort-by-department + sort-by-time copies, e.g. 40 rendered rows = 2×18 patients + 2×2 OPEN).
- Rows: `[class~="filled-appointment-row"]` (app-side fallback wording: `li.filled-appointment-row`).
- Provider header inside each container: `[class~="appointment-header2"]`; single-provider scope proven only when every non-empty container has exactly one header and all normalize to one name (background.js 4587–4614).
- OPEN/capacity slot filter: strip times then `/^(?:(?:\d+\s*(?:min(?:ute)?s?|mins?)\s*)?)(?:open|blocked?|hold|unavailable|lunch|closed|administrative|admin|reserved)(?:\b|\s)/i`.
- appointmentId/providerId from attrs `data-appointment-id|data-appt-id|data-appointmentid|appointmentid` / `data-provider-id|data-rendering-provider-id|data-resource-id|providerid`, else from `href` via `/[?&#/](?:appointmentid|appointment_id|appointment|apptid)[=/:]([a-z0-9_-]{2,})/i`.
- Dedup of the duplicate sort copies: by appointmentId first, else `time|normName|providerKey|proofKey` (v2.9.9 merge-level dedup: live 50 rows→25, `dupRowsRemoved:25`).
- No text-length cutoff — v2.9.20's generic fallback discarded a >300-char row and failed closed at 17/38 (`FABLE_EXTENSION_HANDOFF_2026-07-14.md` §3).

**B2. Modern React grid (v1.48 "structure-id" lane, certified through v2.9.x):** appts are `[class*="PatientAppointment_appointment-container"]` buttons (unique `id`, scroll-invariant dedup key) inside `[class*="ScheduleColumn_schedule-column"]` sections; provider assigned by matching the column section's x-center to headers matching `/^[A-Z][A-Za-z''.\-]+_[A-Za-z].*_(MD|DO|PA-?C|NP|CRNA|APRN|DPM|DDS|DMD|CRNP)\b/`. Source: `extension-v159-prep\src\background.js` 2413–2441.

**B3. Virtualized-grid scroll scrape (v1.46 "coord-scroll"):** find the widest `overflow-x:auto|scroll` element with `scrollWidth>clientWidth+50 && clientWidth>300`; step `scrollLeft` by `max(160–200px, clientWidth×0.45–0.55)`, dispatch `new Event('scroll',{bubbles:true})`, wait **780–820ms** per step (CFG `scrollStepFrac`/`scrollWaitMs`), max 40 steps, re-collect each step, dedupe `provider|time|name`, restore original scrollLeft. Only viewport rows exist in the DOM. Same file 2442–2489.

**B4. Schedule date header:** `h1.fe_c_heading--subsection`, parsed as `new Date(text.replace(/^[A-Za-z]+,\s*/,''))`; fallback header scan for `/^[A-Z][a-z]+day,\s+[A-Z][a-z]+\s+\d{1,2},\s+20\d\d/`.

**B5. Frame pick is content-scored, not URL-dependent:** clock-times regex `/\b\d{1,2}:\d{2}\s*(a\.?m\.?|p\.?m\.?)?/gi` count ×2 (strongest), +25 URL hint `/schedul|calendar|appointment|booking|frontoffice|dashboard/`, −20 messaging words; **v2.9.8 hard-excludes stm.esp/coordinator/messaging/letters frames at 3 layers** (per-frame reader empty, DOM parser bail, scorer −500) — a staff-message thread had been parsed as a phantom 4:00 PM appointment (`EXT_V295_QUIET_PULL_HANDOFF.md`).

**B6. Name quality fixes proven live:** comma-suffix strip BEFORE shape matching (`,?(Jr|Sr|II|III|IV|Esq)\.?` — "…, Jr" was parsed as LAST,FIRST losing the first name, v2.9.7); "City, PA|MD" location-vs-credential guard (state abbrs pass the credential regex); canonical `mlsParseName` cutover only after shadow evidence ≥3 schedule dates / ≥1000 distinct rows / all diffs explained (v2.9.10–13; shadow caught a rev2 reason-tail bug live before output was touched).

## C. Certified patient-open → chart-read → visits loop (params + receipts)

**C1. Opener = findpatient.esp, schedule-independent** (v1.78–v1.88; fixed "0-for-17 → 20-of-21" same day, `DAY_REPORT_2026-07-10_PERFECT_LOOP.md`). Current live code: `release-b273-integration\background.js` `mlsFindPatientOpenDriverFn` (~9429):
- Navigate the content frame (deepest same-origin frame, area>150,000px², SKIP `/globalnav|statusbar|stm\.esp|schedulenavclose|coordinator\/enterprise|blank\.html/i`) to `"{practicePrefix}client/findpatient.esp?filtertype=NAME&findtext=" + encodeURIComponent("last,first") + "&defaultaction=" + prefix + "client/clientsummary.esp" + "&CSRFPROTECT=" + tok`. Prefix = `/^\/(\d+)\/(\d+)\//` from any frame path; CSRF harvested from frame URLSearchParams. **Bare navigation without findtext renders a form-less server error page.**
- Search shape: **`lastname,firstname` NO space**, first name = first token only; strip `\([^)]*\)` nicknames from the search string (v1.90); strip generational suffixes; compound-surname retry ladder: primary split → last-TWO-token surname → lastname-only + DOB row disambiguation (v2.9.6, live-proven on a 3-token compound surname).
- Poll page ready 400ms up to 14s (`input[type=text]` exists); settle 900ms; fill via **native value setter**, re-query the node each try (page init re-renders and clears it), verify the value stuck, click Find; **wait for result ROW hydration** — "N results found" text renders before row links exist.
- Match tiers: exact → registered-nickname ("Robert (Bob)") → prefix; **verify the row's DOB (from the app's own patient store) before opening**; MRN narrows duplicates only after name+DOB veto; 0 or >1 candidates after all tiers = honest refusal.
- Click the row's Chart link — a `javascript:redirectToAirlock(...)` URL that Chrome silently refuses from the isolated world: **the click must run in the page's MAIN world** (v1.83, "the single line between every open failing and every open working").
- v1.94 dob-mismatch override: a stored junk DOB with a single exact-name match may open read-only (save gates still verify the banner).

**C2. Schedule-row click fallback:** athenaOne v26.3 rows are React-wired `<div>`s — bare `.click()` is a NO-OP. `realClick()` = dispatch `pointerover, mouseover, pointerdown, mousedown, pointerup, mouseup` then `.click()`; click the **smallest matching descendant** (live-proven target `DIV.name`); BAD-blocklist only a CONTROL's own short label (<34 chars) — never the whole row text (v1.64: 42/52 rows self-disqualified by their own "Check-out" status; morning runs worked, afternoons failed). Two-athena-tab race: score tabs (`athenanet.athenahealth.com` +100, clinical URLs +40, login/oauth/www-portal −200) — a demdex-tracker portal tab was being scanned instead (v1.65).

**C3. Chart-ready gate (exam-prep/briefing escape):** schedule clicks land on `/ax/appointment/<id>/briefing` exam-prep with **no patient banner** (only provider + dates). `mlsEnsureClinicalChartFn` clicks ONLY safe controls — the "REFRESH CHART" prompt else a plain "Chart" link, hard BAD blocklist `save|sign|order|bill|check-in|prescri|cancel…`, single click; then WAIT for a real banner (name+DOB), **budget 52s inside the app's 75s bridge timeout**, identity injection timeout **20s** (15s caused false "athena-frozen" reloads, v1.63), foreground during load then restore. Never-loads → honest `exam-prep-stuck`. (`WHAT_CHANGED_v159.md` §2–3.)

**C4. Identity reader:** `mlsReadChartIdentity` (v1.52 rewrite, `extension-v159-prep\src\background.js` 383–519): line-by-line only; banner pass = line with age chip `/\b(\d{1,3})\s*(?:yo|y\/o|yrs?\.?|years?\s*old)\b/i` or (`#\s?(\d{4,})` + bare date); name = lead before `[·|]` or digits, with "LAST, First (f) -" PREFIX fallback (v1.64) and 3-line lookback skipping chip lines ≤8 chars/ALLCAPS (v1.60 — an "ENG" language badge sits between name and demographics); PROVCRED final-token reject `MD|DO|PA|…|PHYS|PT` (v1.59/67); scoring: banner +3, DOB/MRN +2 each, chart-route URL +2, `findpatient.esp|stm.esp|inbox` −4, letters/athenaText/communicator −5, body rect <60px −6 (phantom hidden frames); **never emit DOB/MRN without a name; score<0 frames return NO identity rather than junk** (v1.67 — a 0×0 stm.esp frame had poisoned 5/15 pulls). Shadow-DOM fallback `mlsReadChartIdentityShadow` (v1.78): walk open shadow roots, prefer the label block `First Name Used / Legal First Name / … / Date of birth / Patient ID` (values on the following line; **used name beats legal** — schedules carry it), else the chip line joined with 1–3 lines above; emits only with BOTH name+DOB; fallback-only.

**C5. Freeze-guard cadence + recovery:** athenaOne renderer hard-freezes every **~7–9 consecutive chart reads**; proactive `chrome.tabs.reload` every **5–6 heavy reads** at the between-patients boundary (v1.59 →6+, v1.91 →~5); every injection wrapped in a hard timeout (`mlsExecTO`); on hang → reload + auto-click the athenaNet CSRF interstitial **"Continue"** (`mlsAthenaContinueFn` — session survives; appears on most reloads); go-home serialized via `__mlsGroundBusy`; keep-alive self-re-arms after every recovery reload and athena hello (v1.95/96). Serialize per-patient navigation — never parallel-walk charts in one tab.

**C6. Visibility without focus (v2.9.5 quiet pull, live 2026-07-13):** hidden/occluded athena = rAF 0/s, timers 1Hz then 1/min after ~1 min, **visits pane never hydrates** — reads need the window VISIBLE. `__mlsQp`: park athena in a ~770px right-edge work strip with `focused:false`; an unfocused bounds update **raises** the window without stealing focus; re-assert strip bounds per read; restore at run end (app `mlsAppFocusMlsTab` / 120s-quiet watchdog / alarms backstop). Sampled every 500ms over a 5.5-min 13-patient run: zero focus/visibility changes. Writes keep foreground-for-write. MLS app tab is also throttled to ~1 timer/min when backgrounded — use Worker timers / absolute deadlines (b95 fix; si-1.6.2 `makeAbsoluteDeadlineScheduler`).

**C7. Visits pane (chart → prior visits), v2.9.22 live 31/31 bodies across 5 patients** (memory `ext-v2922-history-lane-and-eol-landmine.md`): briefing frame has ONE `ul.encounter-list.autostart`; of 16 `li.encounter-list-item` only **9 true `previous-visit`** rows count (1 vitals + 2 `future-appointment` + 4 `patient-case`); clicking the `li` is a NO-OP — `div.clickable.accordion-trigger` opens metadata only (~20–60 chars), then `span.slideout-trigger-open` opens a NEW child iframe `.slideout.chart-component.slideout-open` holding the real clinical body. Fail-closed sequence: freeze row key (`data-encounter-id`; rows without it hydrate late/volatile) → accordion → snapshot child-frame set → slideout → require exactly ONE new frame → read only it → identity recheck → close → next. `waitForDetailSurface` must wait for the **iframe contract**, not just the container flip (~200ms early); most-recent encounter's iframe is PRE-CACHED so bind by URL-carried encounter-id first, delta as fallback; cold-retry ceiling 8–10s. Rail toggle: `li.chart-tabs__list-item[data-chart-section-id="visits"]` is a TOGGLE (clicking twice closes it).

## D. Live receipts timeline (what worked when)
- 2026-07-08, ext v1.55 + app b95/b96: day pull **13/13** with DOB, per-visit chart summaries persisted, verified after reload (`DAY_PULL_STATUS_2026-07-08.md`).
- 2026-07-09, v1.66/67: day pull **21/21** on-schedule (6.4 min, zero freezes), **DOB 23/23**; month-leg past-day **12/15 saved, 15/15 DOBs** via `.calendar-nav` week-strip gotoDate (`WHAT_CHANGED_v166_v167.md`).
- 2026-07-10 day, v1.88: **0/17 → 20/21** same day (findpatient opener); shadow identity `via:'shadow-labels'`; same-name pair disambiguated by store-DOB row verify (`DAY_REPORT_2026-07-10_PERFECT_LOOP.md`).
- 2026-07-10 night, v1.98 + b124: certification **23/23 DOB+MRN+clean history+visits**; double-run dedup 1137→1137; all-provider June month 30/30 days, 856 found/276 saved/504 already/0 failed (`CERTIFICATION_REPORT_2026-07-10_NIGHT.md`).
- 2026-07-13, v2.9.5–2.9.14: quiet pull 10/13 (3 pre-existing name/opener classes, later fixed); dedup 50→25; canonical-parser shadow zero real-name diffs (`EXT_V295_QUIET_PULL_HANDOFF.md`).
- 2026-07-14, v2.9.21: legacy grid 40 rendered → 36 obs + 4 OPEN → **18 unique, DOB 18/18**, app receipt add 1/enrich 17; **history 0/18** (exam-prep no-banner + same-frame-name-mismatch) (`FABLE_EXTENSION_HANDOFF_2026-07-14.md`).
- 2026-07-14 ~20:00, v2.9.22 reader lane: **31/31 prior-visit bodies cold** across 5 patients (memory file above).

## E. Ops landmines for the current worktree
- `release-b273-integration\background.js` has **MIXED CRLF/LF** (autocrlf false): harness Edit/Write LF-normalizes the whole file (~9–13k-line diff); MSYS tools strip CRs when piping git blobs — byte-check only; fix with scratchpad `restore-eol.js` after any Edit session.
- Two-agent file: check background.js mtime before every edit (Codex edits the same file); coordinate via `release-b273-integration\CLAUDE_CODEX_EXTENSION_SYNC_2026-07-14.md`.
- Pinned loaded folder is `C:\Users\Micha\Downloads\MLS_Assist_v1.65` (name stale — trust manifest/ping); zero-touch loop = robocopy /MIR → postMessage `mlsDevReload` → reload app tab → version ping.
- History runs from a dashboard park must engage the opener leg themselves (b121 contract; known 2.9.12/13 gap: relay pullChart refused `wrong-chart` instead of findpatient-opening first — `EXT_V295_QUIET_PULL_HANDOFF.md` tail).
- Never weaken identity gates to make counts green; `mlsPlausibleDob`'s today-guard will drop a genuine DOB equal to the schedule date (known conservative tradeoff).

---

# MINED BRIEF — proven live techniques for the MLS Assist / Athena day-grid + history loop

**Paths used below:**
- `V198` = `C:/Users/Micha/Desktop/MLS_EVERYTHING/dispatch-work/restore-point-2026-07-10/extension-v1.98-src/background.js` (certified build)
- `V201` = `C:/Users/Micha/AppData/Local/Temp/claude/C--Users-Micha-Desktop-MLS-EVERYTHING/d9443708-2bcc-416a-830d-989bac76f377/scratchpad/v201/background.js` (unzipped from `dispatch-work/visitfix-v12/MLS_Assist_v2.01.zip`)
- `V2921` = `C:/Users/Micha/Desktop/MLS_EVERYTHING/dispatch-work/ui-unification-20260714/background.js` (the build that got DOB **18/18** live on 07-14)
- `CUR` = `C:/Users/Micha/Desktop/MLS_EVERYTHING/dispatch-work/release-b273-integration/background.js` (current, the 0/17 build)
- `SI` = `.../ui-unification-20260714/feat_mls_schedimport_exact.js` (app-side importer; identical file exists in release-b273-integration)

---

## 0. ROOT CAUSE OF THE 0/17 DOB/MRN REGRESSION (highest-value finding)

- **What changed:** `mlsAttachDobs` was gutted. In V2921 (DOB 18/18 live, per `FABLE_EXTENSION_HANDOFF_2026-07-14.md` §1/§3) it is the **text-proximity pass** (V2921:2941-2965): split frame text into lines, find the line containing the patient's last (and first) name token, take the **first plausible bare `MM/DD/YYYY` on that line or the immediately-following line** ("athena wraps DOB under the name"), validated by `mlsPlausibleDob` which rejects any date equal to **today** ("schedule date bleeding through"). Call site: `appts: mlsAttachDobs(__mlsM.appts, (pick && pick.t) || '')` (V2921:~5879-5881).
- In CUR (v2.9.22+), `mlsAttachDobs` (CUR:2988-3000) is a **pure normalizer** — it no longer searches text at all. DOB/MRN must now come from the strict row-scoped extractor `_scheduleRowProofD` (CUR:5830-5842, injected lane) / `explicitRowIdentity` (CUR:4490-4531, worker lane), which only accepts: labeled text `\b(?:DOB|Date of Birth|Birth Date)\s*(?:is\s*)?[:#-]?\s*<date>`, attributes `data-patient-dob/data-dob/data-date-of-birth/data-birth-date` (+MRN equivalents), `aria-label` with those labels, or `data-testid` ending in `dob`/`date-of-birth`/`mrn`.
- **The legacy Athena day grid (`li.filled-appointment-row`) renders DOB as an unlabeled bare date in row text with no data attributes** — so the strict extractor returns nothing → 0/17. The comment at CUR:2964-2969 documents the (safety-motivated) reason the text pass was removed: neighboring rows'/reason dates can be mis-attached. The fix direction: re-add a **row-scoped** bare-date pass (search only within the `filled-appointment-row` element's own text via `mlsPlausibleDob`, not flat frame text) — that keeps the safety property while restoring the signal the legacy grid actually provides.
- **Downstream 2nd failure (fresh-create path):** in `SI`, `patientIdentity()` (SI:216-228) returns `""` unless the row has DOB or MRN proof. With no proof: `findPatient` returns null AND `patientKey` is empty → `upsertPatient` is never called (SI:640-643), `ext` stays empty → `importKey/appointmentIdentity` returns `""` → `failed++` with `appointment-identity-unresolved` (SI:651-657); `queueHistory` refuses with `missing-source-dob-mrn-proof` (SI:554-557). So 0/17 DOB ⇒ 0 local ids ⇒ 0 history targets. Restoring row DOB fixes both at once.

---

## 1. EXACT day-grid extraction lanes that WORKED

### 1a. Legacy day-grid lane (the 18/18 lane, v2.9.21, live-proven 07-14)
- Containers: `[class~="appointments-container"]` (one `<ul>` per scoped provider list; **can be rendered twice** for alternate sort modes). Rows: `[class~="filled-appointment-row"]`. Provider headers: `[class~="appointment-header2"]` inside the container. (V2921:5411-5495; readable expanded copy at V2921:4362-4389.)
- Per-row: `raw = row.textContent`; time = first `\b\d{1,2}:\d{2}\s*(?:[ap]\.?\s?m\.?)?\b`; name via `mlsParseName(raw)` confident display else legacy `pn()` token parser; slot/capacity filter regex: `/^(?:(?:\d+\s*(?:min(?:ute)?s?|mins?)\s*)?)(?:open|blocked?|hold|unavailable|lunch|closed|administrative|admin|reserved)(?:\b|\s)/i` on `raw` with times stripped (V2921:5451-5454).
- IDs: attributes `data-appointment-id|data-appt-id|data-appointmentid|appointmentid` and `data-provider-id|data-rendering-provider-id|data-resource-id|providerid`, plus href-mined `[?&#/](?:appointmentid|appointment_id|appointment|apptid)[=/:]([a-z0-9_-]{2,})` (V2921:5442-5447).
- Reason: `row.querySelector('[class*="reason"], [data-testid*="reason"], [aria-label*="reason"]')` (V2921:5448-5450).
- Dedup: by appointmentId first, else `time|normalizedName|providerKey`; unresolved rows kept in the completeness denominator (fail-closed). **No text-length ceiling** — v2.9.20 failed 17/38 live because a >300-char row was discarded by the generic fallback and duplicate containers/OPEN rows inflated counts (handoff §3, v2.9.20 live finding).
- Live receipt: 40 rendered rows = 2 copies × 18 patients + 4 OPEN → 18 unique, DOB 18/18, app merge "added 1 / enriched 17" (handoff §1, §3 v2.9.21, commit `bd37e9e`).

### 1b. DOB attach that produced 18/18 (restore this, row-scoped)
- `mlsPlausibleDob(s)` (V198:526-537): `^([01]?\d)[\/\-\.]([0-3]?\d)[\/\-\.](\d{4})$`, year ≥1900, ≤ current year, **rejects date == today**.
- `mlsAttachDobs(appts, text)` (V198:538-562 = V2921:2941-2965): for each appt lacking dob, tokenize name (lowercase, alpha-only, tokens >1 char), find a line containing last token (and first token when 2+), scan `line[i] + ' ' + line[i+1]` with global `\b([01]?\d[\/\-\.][0-3]?\d[\/\-\.]\d{4})\b`, first plausible hit wins. Conservative: no match → no dob.

### 1c. Modern/virtualized grid lanes (for the React shape)
- v1.48 STRUCTURE lane (V198:2301-2321; still present in V2921:5497+): appt buttons `[class*="PatientAppointment_appointment-container"]` (unique `id`, scroll-invariant dedup key) inside `[class*="ScheduleColumn_schedule-column"]`; provider assigned by matching the column section's x-center to header elements matching `/^[A-Z][A-Za-z'’.\-]+_[A-Za-z].*_(MD|DO|PA-?C|NP|CRNA|APRN|DPM|DDS|DMD|CRNP)\b/` (athena renders headers like `Last_First_MD`).
- Horizontal virtualization: find the widest `overflowX:auto|scroll` element with `scrollWidth > clientWidth+50 && clientWidth>300`; step `scrollLeft` by `max(160, clientWidth*0.45)` (structure lane) / `0.55` (coord lane), `dispatchEvent(new Event('scroll',{bubbles:true}))`, sleep **820ms** (structure) / **780ms** (coord) per step, re-collect, restore original scrollLeft (V198:2314-2316, 2353-2361).
- Date header: `h1.fe_c_heading--subsection` → `new Date(text.replace(/^[A-Za-z]+,\s*/,''))` → `out.schedDate` YYYY-MM-DD (V198:2317, 2331-2333).
- Frame pick is content-scored, not URL-bound: clock-time count ×2 (cap 60), scheduling keywords +6, URL hint +25 bonus-only, messaging words −20 (V198:2404-2419). v2.9.8 guard: **never parse schedule from frames whose pathname matches `stm\.esp|\/coordinator\/|messaging|letters`** (fabricated staff-meeting "patient" live catch; V2921:5415-5421).

---

## 2. CERTIFIED patient-open → chart-read → visits loop (v1.98/v2.01, live 20/21 and 13/13)

### 2a. findpatient.esp opener `mlsFindPatientOpenDriverFn` (V198:4092-4296; byte-identical lineage still in CUR:8196+)
- **Must inject `world:'MAIN'`** — result rows' Chart links are `javascript:` hrefs (`redirectToAirlock`); Chrome silently refuses those from the ISOLATED world (v1.83, the big one). Invoked as `mlsExecTO({target:{tabId}, world:'MAIN', args:[name,dob], func:...}, 42000)` (V201:4515).
- Search string shape: `Last,First` **no space** (`searchStr = lname + ',' + fq`); strip `(Nick)` nicknames; strip suffix tokens `/^(jr|sr|ii|iii|iv|v|esq|junior|senior)\.?$/i` (", Jr" searched literally returned 0 results live).
- Content frame: deepest same-origin frame with element area >150,000 px², skipping `/globalnav|statusbar|stm\.esp|schedulenavclose|coordinator\/enterprise|blank\.html/i` (the proven `/f1/f2/f2` slot). Practice prefix harvested from frame pathname `^\/(\d+)\/(\d+)\//`; CSRF token from `?CSRFPROTECT=` on frame URLs.
- Navigate: `prefix + 'client/findpatient.esp?filtertype=NAME&findtext=' + enc(searchStr) + '&defaultaction=' + enc(prefix+'client/clientsummary.esp') + '&CSRFPROTECT=' + tok`. Bare findpatient.esp without findtext = server error page with **no form**.
- Timings: 14s poll (400ms) for `input[type=text]`; settle **900ms**; fill via native value setter (`Object.getOwnPropertyDescriptor(HTMLInputElement.prototype,'value').set`) + input/change events; **re-query the input each try** (page init re-renders/clears it), verify `value===searchStr` before clicking Find (4 tries × 600ms); click button with exact text `Find`; poll 12s (450ms) for `/\d+\s+results?\s+found|no results|cannot leave the find text field blank/i`; then **poll 6s (400ms) for `<a>` with exact text `Chart`** — "N results found" renders before row links hydrate (live false 'no-name-match').
- Row match: from each Chart link, `closest('tr')`, cells' text; row DOB = first `([01]?\d)\/([0-3]?\d)\/(\d{4})` in any cell. Tier 1 exact first-name cell match (incl. `\bnick\b` word-boundary for "Robert (Bob)"), tier 2 prefix; DOB veto with **single-exact dob-mismatch override** retry (stored DOB can be junk — live: stored 12/31/1940 vs athena 05/14/1990 blocked every open until v1.94). Ambiguous → refuse.
- **Return immediately after clicking Chart** — app open bridge budget is ~18s; the READ side's chart-ready gate (52s) is the designated waiter (v1.82).
- Route policy (V201:4443-4560): findpatient FIRST (schedule-independent, verifies name+DOB pre-open), schedule-row click fallback; sticky `__mlsOpenPref`; **never** let the schedule scanner scan the findpatient results page (phantom opens). Set `self.__mlsExpectOpen = {name, at}` after any open (180s TTL) so the next read waits for THAT banner.

### 2b. Chart-ready gate (V201:2741-2831)
- `BUDGET_MS = 52000` (inside app's 75s bridge timeout); identity injection timeout **20s** (15s caused false frozen-reloads on heavy loads, v1.63); 2 consecutive injection timeouts → `mlsRecoverAthenaTab` → honest `athena-frozen-recovered`; poll sleep **2400ms**; briefing nudge (`mlsEnsureClinicalChartFn`, 10s TO) each round; after a nudge click sleep **3200ms**; accept weaker identity only after 42s; expected-name match = ≥2 shared name tokens (`nmm`).
- Briefing detection/nudge (V201:812-846): briefing = URL `/\/appointment\/\d+/` or `/briefing|exam-?prep|qualitypane/`, or visible `REFRESH CHART` text, or `go to exam prep`. Click preference: `refresh` else control with exact text `^(full\s+chart|patient\s+chart|chart)$`. Click recipe = scrollIntoView + pointer/mouse prelude `['pointerover','mouseover','pointerdown','mousedown','pointerup','mouseup']` then ONE `el.click()` (adding 'click' to the dispatch list double-fired). Deny-regex on labels: `/save|sign|order|delete|discard|remove|void|submit|bill|charge|check\s*-?\s*(in|out)|prescri|refill|dispense|cancel|log\s*out|reschedul/i`.
- **Foreground-for-load:** hidden tab is throttled ~9× and a hidden Visits pane **never hydrates** (declared "All Events (31)" with 0 rows for minutes; day6 memory). After nudge click: `chrome.tabs.update(tab.id,{active:true})` + `windows.update({focused:true})`, then restore focus to the MLS tab after the read (V201:2807-2810, 2827).
- Identity readers: `mlsReadChartIdentity` (V198:383-519) — banner pass keyed on age chip `\b(\d{1,3})\s*(?:yo|y\/o|yrs?\.?|years?\s*old)\b` + bare date + `#\s?(\d{4,})` MRN hash on the same/next line; "LAST, First (f) -" prefix validation; look back up to 3 lines skipping ≤8-char chip lines; provider-credential final-token veto; frame scoring (+2 chart-route, −4 `stm.esp|globalnav|statusbar|inbox|messag|findpatient.esp`, −5 letters/athenatext/communicator/fax/printer, −6 body <60px, never emit DOB/MRN without a name). Shadow fallback `mlsReadChartIdentityShadow` (V201:653-772): label block `First Name Used`(preferred over Legal)/`Legal Last Name`/`Date of birth`/`Patient ID` value-on-next-line, else chip line + name joined from 1-3 lines above; only from poll ≥2 or post-nudge; 8s race + 6s cooldown cache; identity only with BOTH name+dob.

### 2c. Visits pane reader (V201:5719-5960; live 13/13, then 31/31 bodies in v2.9.22)
- Rail: labels are CSS `::after` content — invisible to textContent. Selector: `li.chart-tabs__list-item[data-chart-section-id="visits"]` (siblings live-enumerated: browse/allergies/problems/medications/vaccine/vitals/results/visits/history/p4p/careManagement). Attr-first, text-scan fallback restricted to left rail `r.left <= 260`. Rail deadline 12s (700ms steps). **The rail LI is a TOGGLE** (day6b) — don't re-click an open drawer; drawer-open state readable from `div[data-chart-section-id="history"]` x-position (54 open / −420 collapsed).
- Pane wait: poll 16s (500ms) for `/visits\s+and\s+cases/i` in body innerText; on miss: Escape (the LI click can land on the top-nav Calendar menu), re-click, wait 10s more; then sleep **1200ms** hydration; "Show: All Events" only via a real `<select>` native setter + change (custom dropdowns skipped); row shape = two lines: `<type>` then `MM-DD-YYYY, Provider, Cred, Specialty` (`/^([01]?\d)-([0-3]?\d)-(\d{4})\s*,\s*(.+)$/` + slash/dot variant), range-validated.
- **Honesty invariant:** the clientsummary "Visits and Cases" summary CARD satisfies the heading with zero rows — ok-empty allowed ONLY with explicit zero (`All Events (0)` / "no visits..."); declared N>0 with 0 parsed rows → retryable `rows-not-parsed`.
- Handler: single-flight `__mlsVisitsBusy` (parallel MAIN-world drivers on one tab = proven freeze-maker), ONE top-frame MAIN injection, **90s total budget**, foreground-for-read, reads count toward freeze-guard.

### 2d. Freeze-guard / recovery cadence (V201:864-910, 4450-4459)
- Athena reliably freezes after ~5-9 rapid chart opens+reads. Proactive: `__mlsReadsSinceReload >= 5` before next open → `mlsRecoverAthenaTab` ("breather" reload). Also `>= 6` on the schedule-read path (V201:2096).
- `mlsRecoverAthenaTab`: `tabs.reload` → wait status complete (20s cap) → sleep 1500ms → up to 3× clear CSRF interstitial (`mlsAthenaContinueFn`: body matches `/unable to complete|could not complete|session (has )?(expired|timed)|please try again/i`, click ONLY exact-text `Continue`; 2200ms between tries) → reset counter → **re-arm keep-alive**.
- Keep-alive: page-context MAIN-world Worker ticking **55s**, synthetic 1px mousemove + scrollBy(0,1)/(0,−1); idempotent (`window.__mlsKeepAlive.armed`), re-armed after every recovery and on athena content-script hello (8s dedupe window). Dies on every navigation — must be re-armed by the worker, not trusted.
- Every executeScript wrapped in `mlsExecTO` (Promise.race hard timeout; a frozen renderer hangs injections forever — the "gohome-failed ×17" class).

---

## 3. LIVE-VERIFIED RECEIPTS (dates/counts)

- **07-10 day:** pull 0/17 → **20/21** after findpatient-first + shadow reader (memory `day4-2026-07-10-perfect-loop`). Root cause of 0/17 then: opener depended on the displayed schedule; dashboard sat on the wrong department.
- **07-10 night:** v1.98 **CERTIFIED** (memory `night4-2026-07-10-certified`; report `dispatch-work\CERTIFICATION_REPORT_2026-07-10_NIGHT.md`; architecture map `dispatch-work\EXTENSION_ARCHITECTURE.md`). 5/5 open+read incl. the previously failing trio; freeze-guard each ~5 heavy reads proven.
- **07-11:** v2.03/2.04 foreground-for-read fixed first-try `rows-not-parsed` — 6/6 first-try visits reads (memory `day6-2026-07-11-v204`).
- **07-14:** v2.9.21 legacy grid: 40 rows → 18 unique, **DOB 18/18**, import "add 1 / enrich 17"; history **0/18** (exam-prep no-banner + `same-frame-name-mismatch`) — `FABLE_EXTENSION_HANDOFF_2026-07-14.md`. v2.9.20 had failed closed 17/38 (long-row + duplicate-container issue, NOT a scroll miss).
- **07-14 ~20:00:** v2.9.22 prior-visit body lane **31/31 cold across 5 patients** (memory `ext-v2922-history-lane-and-eol-landmine`), readerVersion `2.9.22-visits-r4-two-stage`.
- v1.55 is the documented 13/13 history baseline: `dispatch-work\extension-v155-prep\src\`; comparison in `dispatch-work\LIVE_TEST_v157_2026-07-08.md`.

---

## 4. ATHENA DOM SHAPES / GOTCHAS (all live-verified)

- **Grid variants:** legacy `ul.appointments-container` > `li.filled-appointment-row` (+ `appointment-header2` provider headers; duplicated container for alt sort) vs modern `ScheduleColumn_schedule-column` / `PatientAppointment_appointment-container` (virtualized both axes). Structure sniff selector: `'[class*="PatientAppointment_appointment-container"], [class*="ScheduleColumn_schedule-column"], [data-testid*="schedule-grid" i], [aria-label*="schedule grid" i], [class~="appointments-container"], [class~="filled-appointment-row"]'` (CUR:5401).
- **Schedule click lands on exam-prep/briefing** (URL `/appointment/<id>`, "REFRESH CHART" prompt, provider-only header, no patient banner) — must nudge into the clinical chart before any read.
- **Shadow DOM:** clientsummary/airlock banner lives in open shadow roots; `body.innerText` never contains the name; banner splits "First M" / "LASTCAPS" across elements — join 1-3 lines above the chip line.
- **Hidden-tab hydration:** background athena tab never hydrates pane row lists; foreground for load/read, restore focus afterward. Later v2.9.5 refinement: pulls open athena in an **unfocused work strip** (`focused:false`; unfocused bounds-update raises without stealing focus) — memory `ext-v295-quiet-pull`, `dispatch-work\EXT_V295_QUIET_PULL_HANDOFF.md`.
- **Briefing visits list (v2.9.22 ground truth):** one `ul.encounter-list.autostart`; 16 `li.encounter-list-item` = 1 vitals + 2 `future-appointment` + 4 `patient-case` + 9 true `previous-visit`; only previous-visit rows carry stable `data-encounter-id`. `li` click is a NO-OP; `div.clickable.accordion-trigger` opens metadata only; `span.slideout-trigger-open` opens a NEW child iframe `.slideout.chart-component.slideout-open` with the real clinical body; chart-open **pre-caches the most-recent encounter's iframe** (URL-carries-encounter-id binding first, delta-frame fallback); fail-closed row protocol: freeze key → accordion → snapshot frame set → slideout → require exactly ONE new frame → read → identity recheck → close.
- **findpatient.esp:** results page shows every candidate's name+DOB — must be score-demoted (−4) so it is never adopted as chart identity; never scanned by the schedule row-clicker.
- **Home logo** (re-grounding): `.menuitemlogo`, `[title="athenaOne Home"]`, `[class*="athenaone_logo"],[class*="athenanetlogo"]` — GlobalNav frame only (V198:585-598). Dashboard date nav = `.calendar-nav` week strip; today's tab is literally "Today" (no date).
- **Future rows are bookings, not history** (rows dated after today skipped+counted; v2.04).
- **EOL landmine:** `background.js` in release-b273-integration has MIXED CRLF/LF; harness Edit/Write LF-normalizes the whole file (9-13k line diffs). Run the scratchpad `restore-eol.js` walker after editing; only byte-level checks tell the truth (MSYS tools strip CRs).

---

## 5. PATIENT-CREATE / IMPORT WITH HISTORY (the successful-day flow, `SI`)

- Extension returns rows `{time,name,provider,providerId,appointmentId,reason,dob,mrn}` + `schedDate` + roster receipt; app maps them in `domApptsFromResp` (SI:363-389 — note the 2026-07-01 fix: DOB must be passed through, it was once mapped to `""`).
- Date resolution: per-row parsed date → page-printed date (`_detectSchedDate`) → requested day; requested day outranks a week-banner pageDate; conflicting per-row date → `_wrongDay`, never silently moved. Times normalized for both "12:27 PM" and "14:30"; `start_at` = `_acctWallToUtcIso(date, HH:MM)` America/New_York.
- Identity ladder (SI:216-254): exact local id (only with proof) → `athena-patient:<sourceId>` → `mrn:` → `nd:<normName>|<normDob>`; appointment identity = `appointment-id:` else `slot:patient|date|time|provider`. **A display name alone never creates/binds a patient.**
- Fresh create: `stableId(patientKey)` = FNV-1a hash → `p_sched_<base36>`; `upsertPatient({id, name, dob, mrn/athenaId, source:'athena-schedule', created})` (SI:639-649); repeat pulls are **enrichment**: fill only empty fields (dob/provider/athena ids/reason/patient_external_id/start_at) via `/api/appointments/<id>/update`; conflicts preserved for human review. Idempotency ledger in localStorage per day (`schedImportIndexV1::<day>`, 45-day window, pending TTL 5min, claim/rollback).
- History queue (SI:540-572): requires resolved `patient_external_id` AND row DOB/MRN proof matching the local record; emits `{patient_external_id, _mlsTargetPatientId, _mlsTargetDob, _mlsTargetMrn, name, date, source:'athena-schedule-history'}`. Failure reasons surfaced: `patient-not-resolved`, `missing-source-dob-mrn-proof`, `source-proof-conflict`, `appointment-identity-unresolved` — with 0/17 DOB every row dies here, which is exactly the reported fresh-create symptom.
- Never re-blocking confirm: the old `_importPulledSchedule` `window.confirm` both blocked the thread and let `_pullAllHistories` run with zero imports (SI header, root causes 1-3) — the replacement is non-blocking by design.

**Bottom line for the fix:** restore a row-scoped bare-date DOB pass (v1.51 `mlsPlausibleDob` semantics applied to the `filled-appointment-row` element's own text) into `_scheduleRowProofD`/`mlsAttachDobs`, keep the labeled/attribute lane as the preferred source, and the fresh-create + history-queue path unblocks without touching any identity gate. For the history loop itself, reuse the V201 sequence: findpatient MAIN-world open (return-on-click) → 52s chart-ready gate with briefing nudge + shadow fallback + foreground-for-load → visits rail by `data-chart-section-id` → pane honesty gate — with the ~5-read freeze-guard reload and self-healing 55s keep-alive.

---

Mining complete. Structured brief follows.

---

# ARCHIVE MINING BRIEF — proven schedule-DOB / open-read-visits / import techniques

## SECTION 1 — HOW OLDER VERSIONS GOT DOB (and MRN) FROM THE DAY SCHEDULE GRID

### 1.1 THE KEY FINDING — the DOB 18/18 mechanism was TEXT-proximity attach, and it was REMOVED at HEAD
- **Technique:** v1.51 `mlsAttachDobs(appts, text)` — after the DOM scrape returns `{time,name,provider}` rows, scan the schedule frame's **flat innerText** line-by-line; find a line containing the patient's last-name token (and first-name token when 2+ tokens); accept a plausible bare `M/D/YYYY` date **on that line OR the immediately following line** ("athena wraps DOB under the name"). Conservative: no match → no dob, never guessed.
- **Exact code (as shipped in v2.9.21, the DOB 18/18 build):** commit `bd37e9e` `background.js:2941` (verified via `git show bd37e9e:background.js` in `C:\Users\Micha\Desktop\MLS_EVERYTHING\dispatch-work\release-b273-integration`); identical copy live at `C:\Users\Micha\Desktop\MLS_EVERYTHING\dispatch-work\live-legal-release-20260714\background.js:1935-1959`:
```js
var DATE_RE = /\b([01]?\d[\/\-\.][0-3]?\d[\/\-\.]\d{4})\b/g;
// per appt: tokens = name.toLowerCase().replace(/[^a-z\s]/g,' ').split(/\s+/).filter(len>1)
// line must contain last (and first if 2+ tokens); hay = lines[i] + ' ' + (lines[i+1]||'')
// first DATE_RE hit passing mlsPlausibleDob wins
```
- Guard `mlsPlausibleDob` (same file :1923-1934): strict `^M/D/YYYY$`, year ≥1900, not future, **and rejects a "DOB" equal to today** ("the schedule date bleeding through").
- Call sites: `mlsAttachDobs(__mlsM.appts, pick.t)` on the schedule response (current worktree `background.js:6354,6356` still calls it — but see next bullet).
- **Live receipts:** v2.9.21 (bd37e9e, 2026-07-14): legacy Wednesday grid 40 rendered rows → 36 patient observations + 4 OPEN → 18 unique patients, **DOB 18/18**, app receipt add 1/enrich 17 (`release-b273-integration\FABLE_EXTENSION_HANDOFF_2026-07-14.md` lines 5, 13, 87-90).
- **The regression at HEAD (`a455b21`):** `mlsAttachDobs` was rewritten to a pure normalizer — it **no longer searches frame text**; DOB/MRN must arrive already attached per-row by the new strict `_scheduleRowProofD(row)` (current `background.js:5830`), which accepts ONLY (a) data attributes `data-patient-dob/data-dob/data-date-of-birth/data-birth-date` (+ mrn variants), (b) `aria-label`/`data-testid` labeled values, or (c) text explicitly labeled `DOB:`/`Date of Birth:`/`MRN:` inside the row. The legacy `filled-appointment-row` grid renders DOB as **bare unlabeled text near the name**, so the strict extractor finds nothing → exactly the 0/17 symptom. The comment at current `background.js:2964-2969` documents the deliberate rationale (neighbor-DOB adoption risk) — the proven middle ground the archive supports: keep proof **row-scoped** but accept a **bare plausible date inside the row element's own text** (the per-row equivalent of the v1.51 rule), rather than requiring labels/attrs that this Athena skin never renders.

### 1.2 Legacy day-grid selectors (the certified v2.9.21 lane)
Source: current `background.js:5890-5975` (lane `legacy-day-grid`), older equivalent `live-legal-release-20260714\background.js:3361-3388`.
- Containers: `[class~="appointments-container"]` (one `<ul>` per scoped provider list; **the dashboard renders the day TWICE** — sort-by-department + sort-by-time copies).
- Rows: `[class~="filled-appointment-row"]` (`li`; app-side note "li.filled-appointment-row -> {time,name,provider}" in `feat_mls_schedimport_exact.js` header).
- Provider header inside each list: `[class~="appointment-header2"]`; single-provider scope proof = every non-empty container has exactly one header and all normalize to the same name.
- Row attribute mining (v2.9.21): `data-appointment-id, data-appt-id, data-appointmentid, appointmentid, data-provider-id, data-rendering-provider-id, data-resource-id`, plus `href` regexes `[?&#/](?:appointmentid|appointment_id|appointment|apptid)[=\/:]([a-z0-9_-]{2,})` (`background.js:5904-5910`).
- Reason: `row.querySelector('[class*="reason"], [data-testid*="reason"], [aria-label*="reason"]')`.
- Slot/OPEN exclusion: `/^(?:(?:\d+\s*(?:min(?:ute)?s?|mins?)\s*)?)(?:open|blocked?|hold|unavailable|lunch|closed|administrative|admin|reserved)(?:\b|\s)/i` on the time-stripped row tail.
- Dedup: appointmentId-first, else `time|name|provider|proofKey`; unresolved (no time or <2 name tokens) rows stay in the completeness denominator → fail closed. **v2.9.20 lesson:** the generic fallback's 300-char row cutoff silently dropped a real >300-char row → 17/38; v2.9.21 removed the cutoff (handoff §3).

### 1.3 Modern/virtualized grid (structure lane, v1.48+)
Old `background.js:4357-4400` (live-legal-release), current ~5990-6050:
- Appt buttons: `[class*="PatientAppointment_appointment-container"]` (UNIQUE id — dedup by id is scroll-invariant) inside `[class*="ScheduleColumn_schedule-column"]`; provider assigned by matching the column section's x-center to the nearest provider header center.
- Provider header regex: `/^[A-Z][A-Za-z'’.\-]+_[A-Za-z].*_(MD|DO|PA-?C|NP|CRNA|APRN|DPM|DDS|DMD|CRNP)\b/` (headers render `Last_First_MD`).
- **Virtualization:** only viewport columns are in DOM → find the widest `overflowX:auto|scroll` element with `scrollWidth>clientWidth+50 && clientWidth>300`, step `scrollLeft` by `0.45×clientWidth` (coord lane 0.55), **wait 820ms** (coord 780ms) per step, collect each step, restore scroll. Vertical variant scores scrollables containing appointment shapes (current :6045).
- Date header: `h1.fe_c_heading--subsection` → `schedDate` (strip leading weekday, `new Date`).
- Name parse from container text: `_nmSCore` = strip suffixes, then `/,\s*([A-Z][A-Za-z'’.\-]+(?:\s+[A-Z][A-Za-z'’.\-]*)?)\s*(?:\(|$)/` then `/([A-Z][A-Za-z'’-]+)\s*,\s*([A-Z][A-Za-z'’-]+)/`, else token parser. v2.9.20 replaced first-capture-only with canonical `mlsParseName` (comma-name = "Last, First"; the legacy regex kept only post-comma text → one-token names silently rejected).

### 1.4 Schedule-lane hard-won gotchas (all live-verified, memory `ext-v295-quiet-pull.md`)
- Comma-SUFFIX strip before name matching (live row "Lawrence J Dipietrae, Jr" parsed as name "Dipietrae, Jr" → identity gate refused every run; v2.9.7).
- "City, PA|MD" guard — state abbreviations pass the credential regex, a location became a "provider" (v2.9.7).
- **stm.esp/staff-messaging frames must be hard-excluded from schedule scoring** — a staff-message thread was parsed as a 4:00 PM appointment (fabricated-appt class, v2.9.8; 17 phantom rows later deleted).
- Merge-level dedup 50→25 rows live with `providerDiag.dupRowsRemoved` stamped; provider-less "hidden-copy shadow" rows dropped only when a provider-attributed twin exists (v2.9.9, current `background.js` mlsMergeSchedule).
- TIME normalization at scrape: canonical `H:MM AM/PM`; meridian-less passes through BARE, never guessed (v2.9.12).
- **Parser cutover rule:** never cut over on synthetic vectors alone — run SHADOW mode with persisted counters (`chrome.storage.local mlsNameShadowTotals`); rev2's reason-tail bug was caught in ONE live pull after 35 vectors missed it; replace debris with a BOUNDARY marker, not a space (v2.9.10-13).
- Frame scoring for the schedule read: content-scored ("appointment", "schedul", "arrived", "checked in", "exam room", "no show", "walk-in" +6 each; URL hint `/schedul|calendar|appointment|booking|frontoffice|dashboard/` +25 bonus only) — find the schedule by what's IN it (old `background.js:1059-1067`).

## SECTION 2 — CERTIFIED PATIENT-OPEN → CHART-READ → VISITS LOOP

All from `live-legal-release-20260714\background.js` (v2.9.14-era, lineage of the certified v1.98 loop) unless noted.

### 2.1 findpatient.esp opener (schedule-INDEPENDENT; solved the historical 0/17)
`mlsFindPatientOpenDriverFn` (:6361-6573). **Must run `world:'MAIN'`** — `javascript:` hrefs (redirectToAirlock) silently refuse to navigate from the ISOLATED world (v1.83, memory `day4-2026-07-10-perfect-loop.md`).
1. **Search-string shape: `Last,First` — NO space after the comma**; strip `(Nickname)` parens; pop generational suffixes (`jr|sr|ii|iii|iv|v|esq|junior|senior`) after the comma ("Tom E Hatton, Jr" searched as "Hatton,Jr" → 0 results, v1.90); comma-less 3-token names: last token = surname; **v2.9.6 compound-surname retry** — on no-results with a 3-token comma-less name, retry once with the last TWO tokens as surname (live: "Pennington Zytkowicz").
2. Content frame = deepest same-origin frame with area >150k px², skipping `/globalnav|statusbar|stm\.esp|schedulenavclose|coordinator\/enterprise|blank\.html/`.
3. Navigate to `<practicePrefix>client/findpatient.esp?filtertype=NAME&findtext=<searchStr>&defaultaction=<prefix>client/clientsummary.esp&CSRFPROTECT=<token harvested from live frame URLs>`; bare findpatient.esp without findtext = server error page with NO form (v1.80). Practice prefix = `/(\d+)/(\d+)/` from any frame pathname.
4. Wait ≤14s for `input[type=text]`; **settle 900ms; page init CLEARS the input** — fill via the native value setter (`Object.getOwnPropertyDescriptor(HTMLInputElement.prototype,'value').set`), re-query the node each of 4 tries (re-renders replace it), verify `value===searchStr` before clicking Find (v1.79).
5. Click the `^find$` button; poll ≤12s for `/\d+\s+results?\s+found|no results|cannot leave the find text field blank/`; on blank-error refill once.
6. **Hydration:** "N results found" text renders BEFORE row links exist — poll ≤6s for `a` elements whose text is exactly `chart` (v1.85).
7. Row match tiers: exact first-name cell equality (or first word), **nickname word-boundary tier** (`\bbob\b` matches "Robert (Bob)", v1.87), then prefix; a single tier-1 hit beats tier-2 ambiguity (v1.81).
8. **Verify row DOB before opening** — first `M/D/YYYY` in any cell; mismatch → report `dob-mismatch` with the rowDob (never silently drop: stored DOBs can be junk — v1.94 single-exact dob-override lets the app accept the row DOB while saves stay banner-gated).
9. **Return IMMEDIATELY after the Chart click** — the app's open-bridge budget is ~18s; waiting here starved every open (v1.82). The read side's chart-ready gate is the designated waiter.
10. Never scan the findpatient RESULTS page as a schedule or adopt it as chart identity (URL demoted −4; banned by pathname, v1.81/1.84).

### 2.2 Chart-ready gate (the read side)
(:4752-4830): `BUDGET_MS = 52000` (inside the app's **75s bridge timeout**). Loop: inject `mlsReadChartIdentity` allFrames with **20s injection timeout** (15s caused false frozen-verdicts on heavy loads, v1.63); 2 consecutive injection timeouts → `mlsRecoverAthenaTab` + honest `athena-frozen-recovered` (app retries). `expectName` from `__mlsExpectOpen` (180s TTL); name match `nmm` = ≥2 shared normalized tokens. Accept: expected-name match any via; else banner-via identity when no expectation; at >42s accept best non-negative-score candidate or return null (app gate falls back to name-tokens-in-chart-text + schedule-DOB). Shadow-DOM identity fallback fires only from poll 2 / after a nav click, or when the classic candidate is a weak non-banner grep ≠ expectName (care-team names live in chart light DOM). If not ready: run `mlsEnsureClinicalChartFn`, and after a click sleep 3200ms.

### 2.3 Exam-prep/briefing escape (`mlsEnsureClinicalChartFn`, :2210-2247)
Briefing detection: URL `/\/appointment\/\d+/` or `briefing|exam-?prep|qualitypane`, or controls "REFRESH CHART"/"Go to Exam Prep" present. Click ONLY `refresh chart` (preferred) or exact `^(full\s+chart|patient\s+chart|chart)$`; BAD blocklist `/save|sign|order|delete|discard|remove|void|submit|bill|charge|check\s*-?\s*(in|out)|prescri|refill|dispense|cancel|log\s*out|.../`. Scan via **textContent, not innerText — the all-frames innerText layout walk froze athenaOne at 78s**. Click = scrollIntoView + pointer/mouse prelude (`pointerover,mouseover,pointerdown,mousedown,pointerup,mouseup`) + ONE `.click()` (adding 'click' to the dispatch list double-fired).

### 2.4 Freeze-guard cadence + recovery
- athenaOne renderer hard-freezes after ~5-9 consecutive chart opens/reads (live 07-08 and 07-10). Proactive guard: `__mlsReadsSinceReload >= 5` → reload-recover BEFORE the next open (:6656-6664, v1.91). Bulk pulls chunk at this natural boundary (older practice: chunks of 4-7 with health checks, memory `day-pull-v120-b95.md`).
- `mlsRecoverAthenaTab` (:2266-2296): athena-tab-only rail → `tabs.reload` → wait status complete (≤20s) → sleep 1500ms → up to 3× `mlsAthenaContinueFn` (clicks a button whose EXACT text is "Continue" only when `/unable to complete|could not complete|session (has )?(expired|timed)|please try again/` is present; session survives) with 2200ms between → reset counter → **re-arm keep-alive**.
- Keep-alive (v1.95/96, :2297-2320): page-context world MAIN Worker ticking 55s → synthetic mousemove + 1px scrollBy up/down; **dies on every reload/nav** — re-armed after every recovery and on every athena content-script hello while MLS is open (8s dedupe only; a 60s throttle left it dead after back-to-back navs).

### 2.5 Focus/visibility (quiet pull, v2.9.5 — memory `ext-v295-quiet-pull.md`)
- Reads genuinely require a VISIBLE page: fully-covered/minimized Chrome = `visibilityState:hidden`, rAF 0/s, timers 1Hz then ~1/min. Hidden-tab chart loads are throttled ~9×.
- **KEY TRICK:** an unfocused `chrome.windows.update(bounds)` RAISES the window above what covers it WITHOUT stealing focus — the narrow right-edge work-strip window is created/placed `focused:false` and its bounds re-asserted per reuse. No focus debt → the v1.74 guardian never yanks. Release on `mlsAppFocusMlsTab`, 120s-quiet watchdog, alarms backstop, and before any write.
- App-side loop timers must run on a **Web-Worker timer** — Chrome intensive throttling clamps background main-thread setTimeout to ~1/min (b95 fix, memory `day-pull-v120-b95.md`).

### 2.6 Visits reading
- Left-rail Visits item: `li.chart-tabs__list-item[data-chart-section-id="visits"]` — **labels are CSS `::after` content, NOT textContent** (text scan fails; v1.89.1). Read is identity-gated with a retry-until-match loop because the airlock banner renders ASYNC after a findpatient open (v1.89.3). Banner name candidates must include ALL variants (split on "Legal:", strip parens) — collapsed "Robert (Bob) …" vs expanded "… Legal: Robert …" (v1.89.4/5). (memory `day4b-2026-07-10-b121.md`)
- Briefing visits DOM (Athena v26.3, live-verified, memory `ext-v2922-history-lane-and-eol-landmine.md`): ONE `ul.encounter-list.autostart`; example census 16 `li.encounter-list-item` = 1 vitals + 2 `future-appointment` + 4 `patient-case` + 9 TRUE `previous-visit`; completeness counts ONLY previous-visit rows; only they carry stable `data-encounter-id`. Clicking the `li` is a NO-OP: `div.clickable.accordion-trigger` opens metadata only; `span.slideout-trigger-open` opens a NEW child iframe (`.slideout.chart-component.slideout-open`) with the real clinical body. Fail-closed sequence: freeze row key → accordion → snapshot child-frame set → slideout → require ONE new frame → read only it → identity recheck → close → next. `waitForDetailSurface` must wait for the **iframe contract** (container flips open ~200ms before the iframe exists). Frame binding: accept URL-carries-frozen-`data-encounter-id` FIRST (chart-open PRE-CACHES the most-recent encounter's iframe so frame-delta proof is unsatisfiable there); delta = fallback. Cold-retry ceiling 8-10s. **Receipt: 31/31 prior-visit bodies cold across 5 patients (1+9+6+4+11), 2026-07-14, readerVersion `2.9.22-visits-r4-two-stage`.**
- App-driven all-visits read budget: `cv._driveRequest('mlsAppReadAllVisits', {}, 'mlsAppAllVisitsResult', [...progress], cb, null, 240000, 12000)` — 240s total, 12s progress-gap (`MLS_athena_autopull_build\feat_athena_autopull.js:151-153`). Bridge contract: `mlsAppReadChart` takes `patient` (not name/dob) and replies `mlsAppChartResult`.

### 2.7 Chart identity readers (what "banner" means)
- Classic `mlsReadChartIdentity` (:1780-1916): line-by-line only; banner pass keys on `AGE_CHIP /\b(\d{1,3})\s*(?:yo|y\/o|yrs?\.?|years?\s*old)\b/`, `BARE_DATE M/D/YYYY`, `MRN_HASH /#\s?(\d{4,})/` (chip shape "NNyo M | MM-DD-YYYY | #NNNNNNN"); name = lead of that line or up to 3 lines ABOVE, skipping ≤8-char chip lines (v1.60); `Last, First` PREFIX validation for "LAST, First (f) -" leads (v1.64); provider-credential final-token veto (`PROVCRED` incl. PHYS/PT, v1.59/67). Frame score: +2 chart-route URL, −4 `stm.esp|globalnav|statusbar|inbox|messag|findpatient.esp`, −5 letters/fax/documentviewer, −6 zero-size body (<60px); **name-less frame → fully blank identity (never leak a DOB/MRN without a name, v1.52)**.
- Shadow reader `mlsReadChartIdentityShadow` (v1.78+): flat-tree walker over open shadow roots; parses the banner drawer label block — `First Name Used` (PREFER over Legal), `Legal First Name`, `Middle Name`, `Legal Last Name`, `Date of birth`, `Patient ID` (values on the FOLLOWING line) — else chip line + name JOINED from 1-3 lines above (banner splits first/last across lines). Emits identity ONLY with name+DOB. `chartMrn`/banner `#id` == findpatient ID column (stable athena patient id, CONFIRMED, v1.89). (memory `day4-2026-07-10-perfect-loop.md`)

## SECTION 3 — LIVE-VERIFIED RECEIPTS (chronology)
- 2026-07-08 (b95, ext v1.55): day pull **13/13** Athena-scheduled patients, 10-14KB chart summaries (`day-pull-v120-b95.md`).
- 2026-07-10 AM: 0/17 (dashboard on wrong department — schedule-display dependence) → findpatient opener → **20/21** same live day (`day4-2026-07-10-perfect-loop.md`).
- 2026-07-10 PM (v1.89/b121): 7/09 cohort 23 patients — **DOB 23/23, history 23/23, 0 dupes**; re-pull added 0 rows 1134→1134 (`day4b-2026-07-10-b121.md`).
- 2026-07-10 night: extension CERTIFIED v1.98; 5/5 open+read incl. freeze-guard cases (`night4-2026-07-10-certified.md`).
- 2026-07-13 (v2.9.5-13): 13-patient quiet-pull day, 10 charts OK, zero focus steals; dedup 50→25; shadow parser 684 checked/0 differs (`ext-v295-quiet-pull.md`).
- 2026-07-14 (v2.9.21 bd37e9e): legacy grid 40→36+4 OPEN→**18 unique, DOB 18/18**, add 1/enrich 17; **history stage 0/18** (fail classes: exam-prep no banner, `same-frame-name-mismatch`) (`FABLE_EXTENSION_HANDOFF_2026-07-14.md`).
- 2026-07-14 (v2.9.22 reader lane): **31/31 prior-visit bodies** across 5 patients (`ext-v2922-...md`).

## SECTION 4 — PATIENT-CREATE / IMPORT-WITH-HISTORY FLOW

### 4.1 The proven find-or-create (as of the DOB 18/18 import)
Old `feat_mls_schedimport_exact.js` (si-1.3.0, `live-legal-release-20260714`):
- `patientIdentity(a)` = `"mrn:"+normMrn` if present, else `"nd:"+normName+"|"+normDob` (**works even with empty DOB — key is still non-empty**).
- `findPatient` tiers: MRN exact → (mrn-less) name+DOB → **name-only last resort** (:163-173).
- Create: `np = { id: stableId(patientIdentity(a)), name, dob, reason, source:"athena-schedule", created }` → `upsertPatient(np)`; `stableId` = FNV-1a hash → `"p_sched_"+hash36` (deterministic → idempotent re-pulls). Backend row carries `patient_external_id` binding appt→local patient.
- Per-day import ledger (`schedImportIndexV1`, keys = identity|date, states pending/done, 5-min TTL, 45-day window) makes re-pulls enrich instead of duplicate. **Warning on file:** ledger keys name+DOB — re-pulling the same day after fixing a bad name ADDS a corrected row beside the stale one (`ext-v295-quiet-pull.md`).
- DOM-scrape fallback mapping **must forward dob/mrn** — a 2026-07-01 fix note: dob was once mapped to `""` here and imports stored dob-less rows (:192-197).

### 4.2 Why the CURRENT fresh-create resolves no local id (archive-grounded diagnosis)
Current `release-b273-integration\feat_mls_schedimport_exact.js`:
- `patientIdentity(a,false)` now returns `""` unless `validDobProof(a.dob)` or MRN exists (:399-409 area — "A new schedule row still needs its own DOB/MRN proof").
- `materializePatient` (:1025-1050): `findPatient` miss → `key = patientIdentity(a,false)` → key empty → **return null** → no local patient → `historyTargets` never gets a `_mlsTargetPatientId` row → history queue starved. So the 0/17 DOB extraction and the no-local-id-before-history-queueing are ONE bug: restore row-scoped DOB capture (per §1.1) and both symptoms clear, without weakening the proof rule. The current `materializePatient` verify-persisted-by-re-reading-`getPatients()` pattern is good and mirrors the b121 lesson (addVisit clobber: always re-adopt freshly-persisted state — `day4b` memory §CONFIRMED BUG).
- Other proven create shapes: app-side `resolvePatient` (`MLS_athena_autopull_build\feat_athena_autopull.js:96-115`) and `createOrFindPatient` (`MLS_addvisits_build\feat_addpatient.js:60-99`): both name+DOB match with "both present → must match, one missing → name suffices," backfill missing dob on the existing record, id = `Date.now().toString(36)+rand`.
- Import blockers fixed before (don't reintroduce): `_importPulledSchedule`'s native `window.confirm` on heavy days blocked the thread and Cancel imported ZERO while histories still ran (si header, root cause 1); non-existent patient-id switch HANGS the app (`MEMORY.md` staged-easy-round2); visit filing must call `M.addVisit(p.id, {...})` directly — `ingestChart` silently lost visits via persist:false-on-clone (b95 root cause 2).
- Day keying: file appointments under Athena's ACTUALLY SERVED date (schedDate from the page header), never the displayed/assumed day (`__mlsDayKeyFix`, standing rule `model-check-and-calendar-day-rule`).

## SECTION 5 — MISC ATHENA DOM/OPS FACTS WORTH KEEPING AT HAND
- Foreground-for-read: a hidden tab's pane never hydrates (`day6-2026-07-11-v204.md`); use the work-strip, not activation.
- `mlsAppGotoDate` returns "No date control recognized" on the clinical schedule; within-week days reachable via week-strip day tabs; week-strip window needed ±1 fudge removed (v1.97 Sunday bug).
- Past-day schedule rows open the FULL-CHART view whose banner frame the old reader missed → honest identity refusals (month-leg design constraint, `day-pull-v120-b95.md`).
- History NOTE editors are contentEditable DIVs — synthetic clears don't persist; only real keys + real blur (write lane, `night4`).
- Repo landmine for the current worktree: `background.js` has MIXED CRLF/LF; harness Edit/Write LF-normalizes the whole file; MSYS tools strip CRs when piping git blobs — verify by bytes only; `restore-eol.js` walker exists in the 07-14 session scratchpad (`ext-v2922-...md`).
- Build/reload loop: mirror to pinned `C:\Users\Micha\Downloads\MLS_Assist_v1.65` (name stale) → postMessage `{source:'mls-app',type:'mlsDevReload'}` → reload MLS + athena tabs (orphaned content scripts answer mlsPong with version '') → re-ping.

Key source files: `dispatch-work\live-legal-release-20260714\background.js` (proven pre-strict reader, lines cited above), `dispatch-work\release-b273-integration\background.js` (+`git show bd37e9e:background.js` for the exact 18/18 build), both copies of `feat_mls_schedimport_exact.js`, `03_handoff_and_reports\MLS_Assist_v1.40_source.zip` (earliest bridge/identity lineage), `release-b273-integration\FABLE_EXTENSION_HANDOFF_2026-07-14.md`, and memory files `day4-2026-07-10-perfect-loop.md`, `day4b-2026-07-10-b121.md`, `night4-2026-07-10-certified.md`, `day-pull-v120-b95.md`, `ext-v295-quiet-pull.md`, `ext-v2922-history-lane-and-eol-landmine.md` under `C:\Users\Micha\.claude\projects\C--Users-Micha-Desktop-MLS-EVERYTHING\memory\`.
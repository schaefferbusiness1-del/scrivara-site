# LIVE PULL EVIDENCE — extension 3.0.24 + app b741 — 2026-07-27

Real signed-in day pull on the owner's machine (account leeschaeffer41@gmail.com,
visible tab, app b741, extension pong `3.0.24+core-sha256:d5da1df3…e578ec`).
All times UTC. This is the (a) leg of the Web-Store evidence walk.

## BEFORE (17:44:04Z, probe on the live tab)

- `sf_u::leeschaeffer41@gmail.com::schedImportIndexV1::2026-07-27` → **ABSENT**
- `schedImportDaysV1` does not contain 2026-07-27 → day never pulled
- Pull button idle: "📥 Pull today"

## PULL (17:44:15Z click — the real #mlsDsPullBtn, no simulation)

Progress panel (`__mlsDayHistoryPull.state`, the b728 pill's data source — this
run is also the end-to-end proof the resurrected progress reporter feeds live):

- 17:45 — running, total **18**, done 1, current "Michael Spagnuola"
- 17:47 — done 2, failed 2 (both: "The Athena patient open reached its one
  absolute deadline during find-patient op")
- 17:50 — done 15, failed 7, current "Sylvester Davis"
- 17:52 — done 18/18 attempted, still finalizing
- 17:57 — **running:false — final: ok 11 / failed 7 of 18**

## AFTER — the receipts (the bar from the pull-verify skill)

- `schedImportIndexV1::2026-07-27`: **18 rows, states {done: 18} — every row
  `state:"done"`, every key a real `appointment-id:`** (18/18 apptIdKeys;
  sample `appointment-id:51050635` → backendAppointmentId 4006, appt_date
  2026-07-27). THE SCHEDULE IMPORT RESOLVED ALL 18 — zero unresolved
  identities, zero guessed rows.
- `schedImportDaysV1` now **contains 2026-07-27** — day marked complete.

## History-read half (the fragile bodies lane)

11/18 verified histories read on the first pass. The 7 failures carry honest,
progressive reasons (2× patient-open absolute deadline; 1× "Could not find the
Athena patient search box on this screen."; 4× "Open your signed-in athenaOne
in another tab, then try again") — root cause: the Athena tab had been
reloaded ~4h earlier into a hollow 62-character dashboard shell (5 empty
frames), which also reproducibly HANGS the v1.51 chart-identity verb (no exec
budget — 3.0.25 hardening candidate). The extension's own navigation rebuilt
the frameset mid-pull, after which reads succeeded. The designed recovery
button "↻ Retry failed histories only (7)" was clicked 17:59:58Z; the retry
pass attempted all 7 with zero immediate refusals (result recorded below when
final).

## RETRY RESULT (18:22Z)

**running:false — retry pass ok 7 / failed 0.** The pass ran ~22 minutes
against a BACKGROUNDED Athena tab (the throttled fragile lane — it looked
frozen for three consecutive polls and then settled; patience, not re-clicks,
was the correct response). Combined day total: **18/18 verified patient
histories read, zero remaining failures.** Schedule ledger unchanged at 18/18
`done` with real appointment-id keys; day remains marked complete.

## LEG (b) — Adam-only write-back walkthrough (18:15–18:40Z)

- selectPatient('mr85n5sdkd6o') → banner follows: "Adam J Schaeffer 20y ·
  DOB 03-24-2006 · MRN 7833832 · 4 visits". Day counter shows "18 / 18 seen".
- `mlsAppReadChart` for Adam: **ok:true, chartName "Adam J Schaeffer",
  4,304ms** — the extension found + opened his real chart in Athena and
  banner-verified identity (another live 3.0.24 read-lane proof).
- History → Adam's Jul-20 record → "Review Athena actions" → the unified
  review opened with the b724 walkthrough strip LIVE ("1 Pick the action → 2
  Athena checks it → 3 Confirm & write → 4 Verify in Athena"). The modal
  pinned the EXACT patient (name/DOB/MRN/MLS id) and expected visit
  7/20/2026 · provider Michael Schaeffer, manifest mls-preview-f30b4016.
- Both writable rows (Write reviewed note, Save draft) reported **BLOCKED:
  "The exact visit needs its date, provider, and appointment ID (or a bound
  encounter ID and URL). MLS will not guess an encounter."** — the
  fail-closed encounter rule proven ON THE REAL SURFACE with real data.
  Billing row correctly MANUAL. This is the owner's review gate working; a
  READY write requires Adam on a real schedule slot ([MLS TEST] booking —
  owner action) or a bound encounter.
- Isolated defect (not a 3.0.24 delta): the v1.51 `mlsAppChartIdentity`
  allFrames injection HANGS on this Athena frameset even with Adam's chart
  open, while targeted injections (read-chart driver) answer in ~4s. All
  dependents fail closed. Fix queued for 3.0.25: execution budget + targeted
  frames (the v1.89 visits-reader pattern).

## RUN 2 (18:42Z click — owner's "fast and perfect" bar, healthy Athena tab)

Same real day, same real button, extension 3.0.24 + app b741 (b742 banner
live). Pace: 3/18 at 69s, 11/18 at 179s, **all 18 attempted by 313s
(~16s/patient) with ZERO hard failures at any point** — including every
patient that refused in run 1's cold-wedge window (Brooks, Spagnuola, Fink,
Bray, and the rest went straight through). Ledger stayed 18/18 done
throughout (idempotent re-pull, no duplicates, no regressions). Finalization
of the pipelined verified reads ran long against the BACKGROUNDED Athena tab
(12+ min elapsed at interim write) — the known occluded-lane throttle, the
one remaining "fast" gap; final settle count appended below.

## VERDICT (leg a)

The pull machinery on ext 3.0.24 + app b741 delivered a COMPLETE real day:
every appointment resolved, every history read, honest per-row refusals on the
way there, and the designed retry closing the gap to zero. The b728 progress
panel fed live data through the entire run (18-patient roster, per-patient
current name, fail/ok flips at finalization).

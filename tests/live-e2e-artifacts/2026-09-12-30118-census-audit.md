# MLS Assist 3.0.118 candidate: full-history census audit

This is synthetic and source evidence. The candidate has not been installed,
run against live Athena, or published. No live clinical write was performed.

## Candidate identity

- Worktree: `dispatch-work/athena-native-persistence-30115`.
- Branch: `codex/athena-native-persistence-30115`.
- Starting commit: `d3fd35b3` (3.0.117).
- Version: `3.0.118`.
- Core SHA-256: `260b0aaae793f0fe5f0843508534e95e4d47feb8b9d46081fc638c78274907bb`.
- ZIP SHA-256: `c604b3c27b9555b4e1b099e8e2e123aee2253788de27c56589114f6bc86ae103`.
- ZIP: `MLS_Assist_v3.0.118.zip`; new staging folder:
  `extension-staging/MLS_Assist_v3.0.118`.
- Two independent builds produced the same ZIP digest. The package has the
  audited 20-file allowlist. Public release metadata remains at 3.0.112.
- The enabled 3.0.117 staging copy was not changed or reloaded.

## Confirmed defect and repair

The ax fallback already knew when its harvest was smaller than the classic
encounter index. With six known rows and four harvested/read bodies, it returned
`complete:false`, `expected:4`, `parsed:4`, and `indexRowsKnown:6`. The shared
terminal hop ignored that refusal and recomputed completion from 4 == 4, changing
the result to `complete:true`.

`ax-history-census-terminal-runtime.test.js` executes the actual fallback closure
and actual terminal block together. It failed on 3.0.117 at the assertion that a
four-of-six history must remain incomplete. The healthy four-of-four control
passed on those same bytes.

The candidate now preserves the larger known full-history population, returns
`ok:false` with `visit-bodies-incomplete` for a partial fallback, and preserves
the prior completeness decision at the final hop. The same example reports
expected 6, parsed 4, attempted 4, not attempted 2, and incomplete. A deadline
reports the number of navigations actually attempted, rather than the planned
cap. This allows the existing retry path to see and retry the partial read.

The new regression also covers caps, deadlines, failed navigation, an identity
refusal, independent completeness refusals, and legitimate zero-body outcomes.
It checks returned synthetic encounter IDs against the operations actually run.
It does not claim that a live ax harvest necessarily contains the whole chart;
that remains a live index/ID comparison requirement.

`scripts/splice-30118.js` makes only unique literal replacements and verifies
their inverse restores every original byte, preserving the mixed line endings
in `background.js`. The native ActionV2 write-driver block is identical to the
3.0.117 staged copy. No site source file was edited.

## Verification

Before the repair, 40 focused existing suites passed. That did not detect the
cross-hop census defect. The new executable regression was red before the fix.

After the repair, 32 focused suites passed:

- The new ax terminal census regression, ax native reader, full visit reader,
  all-visits cleanup serialization, visits surface/cleanup, all five enumerate
  suites, visit-index deduplication, index frame-reload stability and surface
  reporting, full-notes choice gates, history verdict census, scoped additive
  save, day convergence, single-pull accounting, resume/skip/cost, and day/future
  note coverage: 20 suites.
- Native persistence runtime (123 checks), native proof contract, Slate reader,
  active save surface, session preservation, busy keep-alive, session health,
  draft-only execution, legacy executor closure, extension read paths, tab
  resilience, and hidden-safe sleeps: 12 suites.

Syntax, core digest verification, and `git diff --check` passed. This was a
focused extension gate, not a new full-site release gate.

## Reconciled instructions and remaining live proof

- The July 3.0.0 guide and its old worktree/version are historical. Current
  source, recent commits, candidate digests, and enabled-folder evidence govern.
- The August 16 final-action unblocking handoff predates September's draft-only
  execution contract. It is not authorization to enable or test orders,
  billing, signing, or live clinical writes.
- The August 25 day-facts contract supersedes the old claim that turning full
  visit notes off means schedule-only. Chart facts and the pulled day's own
  clinical note remain the minimum; full mode adds historical bodies.
- A future appointment's scheduling notes/procedure reason are distinct from
  its future clinical encounter body. The latter is explicitly classified as
  not yet available. Live op-note preparation still needs proof that the
  upcoming appointment's available source note/reason was captured and carried
  into the generation context.
- Existing site tests explicitly describe a duplicate scoped day-note read
  after successful direct capture. This was reported to the site owner and was
  not changed in this extension patch.
- Root/Sol completed the owner-confirmed Monday September 14 full-mode read on
  site b1258 with installed 3.0.117. Schedule coverage was 26/26. History
  progress reached 22/26, then repair identified three charts for a second
  read. The terminal page said "Pull partial for Mon, Sep 14"; the overall
  Schedule & history pull said "DIDN'T FINISH" after 12m08s. The retry batch
  reported 3 of 15 charts read, 12 failed. The missing-visit-list pass reported
  1 of 3 lists read, 0 visits filed, and 2 unreadable lists. An earlier separate
  chart receipt was 0/1 failed. These are separate visible subjob receipts,
  not an inferred additive census of unique patients.
- Root verified one Athena tab remained open and signed in at the terminal
  result. This run failed acceptance despite schedule coverage and session
  preservation. No live clinical write was initiated.
- 3.0.118 addresses the independently proven fallback census/terminal defect;
  the aggregate failure counts above do not establish that all 12 chart
  failures used that path. Their sanitized per-chart reasons, coverage
  receipts, surface/fallback tags, and timings remain required to attribute
  those failures. Do not claim that this candidate will recover them all.
- On a six-known/four-read ax fallback the candidate must retain expected 6,
  parsed 4, attempted 4, notAttempted 2, indexRowsKnown 6, complete/bodyComplete
  false, fullDetail false, and ok false with retryable visit-bodies-incomplete.
  A healthy four-of-four read must still succeed. A 3.0.118 live comparison
  must account for exact encounter IDs, expected versus saved bodies,
  omissions, retries, appointment-note context, truthful Activity completion,
  and preserved Athena sign-in. It must separately report the pulled day's
  clinical note and the upcoming appointment's available scheduling note;
  not-yet-available future clinical text is not permission to omit the latter.
- No claim of perfect live reads/writes follows from these synthetic results.

# Live E2E pass 1 — 2026-07-20, b442, production, Adam J Schaeffer

Executed autonomously in the signed-in production environment (doctor's Chrome,
real backend, extension 2.9.43) per LIVE_E2E_RUNBOOK_2026-07-20.md, during an
active clinic under the owner's testing directive. All mutations confined to
the approved test patient.

| Step | Result | Evidence captured in-session |
|---|---|---|
| 1 Sign-in / detection | PASS (pre-existing session; ext 2.9.43 handshake; Athena tabs detected) | mlsExtHealth + mlsExtVersion probes |
| 2 Auto context | PASS — booked test appt id 3884 (app's own /api/appointments), engine row-open `startVisitFor('3884')` → screen doctor, locked Adam, scheduled-appointment binding {date 2026-07-20, provider Michael Schaeffer, appointmentId ""} | binding/state probes |
| 3 History | PASS — buildPatientContext 931 chars (identity + problems); chart coverage verified-complete; visits provenance-marked (b442) | context/store probes |
| 4 Capture | PASS — supported paste-transcript lane, 778 chars, identity-bound (manual-entry → scheduled binding) | transcript + binding probes |
| 5 Generate | PASS — REAL production AI, ~60 s, 1,271-char SOAP note; HPI/exam/plan strictly transcript-faithful; PMH correctly drawn from Adam's REAL chart history (lumbar radiculopathy, scoliosis, ibuprofen PRN, NKDA) as background; conservative coding 99213 / S63.501A | note + coding capture |
| 6 Review + save | PASS — fidelity review (no invented content); saveCurrentNote → note n178456216642341c4 under patientId mr85n5sdkd6o, visitDate 2026-07-20, provider, unsigned (signing is human-only) | notes-store probe |
| 7 Write gate | PASS (truthful block) — wf2-1.9.0 unified manifest live: write_note & save_draft `blocked`, reason "The exact visit needs its date, provider, and appointment ID (or a bound encounter ID and URL). MLS will not guess an encounter." — the b440 fix in production; affirmative Athena write requires a real Athena appointment/encounter (owner pass) | manifest capture |
| 8 Athena save verify | PENDING owner pass (requires Adam's real Athena encounter open; exact-tab, one-click confirm) | — |
| 9 Recovery | PASS — hard reload: note + transcript intact and reopenable at full length; the doctor's own active-patient context restored correctly | post-reload probes |
| 10 Next patient | PASS — clinic moved to a new real patient after the test visit; empty workspace, no bleed, no manual restore | post-reload state |

## Findings from this pass

1. **Provider-less schedule rows silently refuse record/generate on TODAY's
   path.** installScheduledVisitBinding requires date+provider; the refusal
   warning is TRANSIENT (computePhase wipes lastWarn ≥30 chars — the b438
   persistent badge exists only on the cross-day path). A doctor on a
   provider-less row gets a first-click refusal with no persistent explanation.
   Normal Athena-pulled rows carry providers, so exposure is edge-case, but a
   persistent badge for today's path would close it. → improvement candidate.
2. Booking with `doctor_user_id` set is required for the row to carry a
   provider; `POST /api/appointments/:id/update` fixes it in place.
3. Tooling: CDP Runtime.evaluate hard-caps at 45 s — poll long operations in
   separate short calls.

## Residue (deliberate)

- Test appointment 3884 ("MLS reliability test visit", Adam, provider Michael
  Schaeffer) remains on today's calendar for the owner's Athena-side pass
  (runbook steps 7–8). Delete via calDeleteAppt(3884) afterward if unwanted.
- Adam's saved test note n178456216642341c4 remains in his MLS history.

# 2026-07-22 acceptance session — history pulling (OFF/ON), ext 3.0.2 release train, Adam-only writeback

Goal: 2 consecutive perfect OFF pulls + 2 consecutive perfect ON pulls → release 3.0.2 → writeback (Adam J Schaeffer only).
Environment: doctor account (leeschaeffer41@gmail.com, 1458 local patients), Athena practice 22724 dept 121 (athenaCollector v26.3 FL), provider roster = ["Matthew Schaeffer, MD"] (sole entry). Extension: unpacked pinned folder at 3.0.2, digest 2fc99a53…f155e5 — byte-identical to repo source, runtime-verified via mlsPong. ONE Athena tab throughout.

## Morning state (pre-session)
- Live site b482; ext 3.0.2 source-only (parts 1–8 committed across e4f52e2 + 391bd46).
- Today (2026-07-22) had already been pulled at ~08:42 EDT by an earlier session: 17/17 appointment rows done, day complete.

## OFF pulls #1/#2 on b482 (09:45, 09:52 EDT) — receipts clean, but DUPLICATES found
Both pulls: 17/17 rows state:done (all `appointment-id:` keys, all updated post-click), day complete, coverage receipts complete+exactIdentityVerified with EXACTLY the six cards (problems, meds, allergies, summary, vitals, history) on all 17, bodies 43→43 (zero new — OFF contract held; zero lost). Timings: pull1 ~5m48s total (history 17/17 in 1m59s), pull2 4m15s (history 2m7s).
**FAIL on the zero-duplicates bar:** pull #1 created 4 exact-duplicate index rows (same date/type/textHead/source, ids ~46ms apart); 2 more pairs existed from 2026-07-21 (p_sched_dt3a1n). Base36-timestamp id forensics pinned creation windows precisely.

## Root cause (field-level, live store)
Surviving pairs were ALWAYS one identityVerified:false row + one identityVerified:true row (binding = patient id), identical content: the SAME history-card row ingested through TWO trust paths ms apart — the base card save files a VERIFIED shell; the __mlsVisitWire post-hook re-ingests chart.visits UNVERIFIED whenever its saveRef proof gate fails. `_trustCompatible` rightly refuses to merge across the trust boundary → both persist. (A second, lesser hazard: `_findPatient` preferred window.findPatient, which can serve a pre-batch clone.) Boot hydration from the server mirror also pruned 5 divergent rows between sessions (two stores, different last-writer per patient) — the 3 pairs that survived were consistent in both stores.

## Fixes shipped
- **b483** (391bd46): `_findPatient` batch-aware (getPatients first); `_collapseExactIndexDuplicates` in addVisit + deterministic post-batch heal at end of ingestChart; feat_visits pin vis7; new suite tests/visit-index-dupe-collapse.test.js (REAL store semantics: replace-upsert, frozen findPatient snapshot).
- **b484** (abe07a9): collapse regrouped by exact CONTENT with trust-twin rule — verified rows with ONE binding win, unverified twins dropped (nothing upgraded), conflicting bindings fail closed, pure-unverified groups keep earliest; pin vis8. Full 260-suite gate green both builds. Both live-byte-verified (app-version.json + script src on the doctor tab).

## OFF pull #1 on b484 (10:36 EDT) — FAILED: Athena session expired mid-pull
Schedule phase fine (18 rows now — an 18th appointment was added mid-clinic; 17 updated post-click). ALL 17 history reads refused: first "The Athena patient open reached its one absolute deadline during find patient open", rest "deferred after timeout". Retry-failed-histories (10:41) refused all 17 again in ~2m. Screenshot proof: the Athena tab was sitting on identity.athenahealth.com sign-in (mschaeffer12 pre-filled) — the athenaNet session idle-expired between 09:52 and 10:36. The refusals were honest; no wrong-patient data, no partial saves claimed. Owner notified by push to sign in (credentials are owner-only).

### Queued improvement (not blocking acceptance)
The pull/retry pre-flight verifies the MLS backend session (/api/me) but not the ATHENA session; an Athena sign-out surfaces as vague per-patient deadline refusals. The conn-truth probe already distinguishes a signed-in Athena tab — wiring it into the pull/retry pre-flight would name "Athena signed out" honestly. Do post-acceptance.

### Instrumentation note
During post-fix pulls, addVisit is wrapped in-page (window.__accDupEvents) to flag any add leaving an exact-content collapsible pair on the touched patient — zero events so far (pull aborted before history writes).

## OFF-mode ACCEPTANCE PASSED on b486 (12:26 + 12:33 EDT)
Two consecutive perfect OFF pulls: 18/18 rows done (all appointment-id keys, updated post-click), day complete, coverage complete + exactIdentityVerified + exactly 6 cards on all 17 patients, bodies 43→43 (zero new — OFF contract; zero lost), ZERO duplicate pairs (and the 3 stranded trust-twin pairs self-healed during the first b485-era pull — store clean since), no incomplete banner. Timings 4m26s / 4m36s (history phase ~2m11s / 2m17s). Refresh persistence verified: identical receipts/counts after a full reload (b486, 1459 patients, 1955 visits, 43 bodies, 0 pairs).
Also shipped en route: **b485** (zero-add ingest heal — the b484 tail only ran when a batch added rows, which left stranded pairs untouched through a clean pull) and **b486** (si-1.8.1 probe-gated transient-runner-death recovery, both lanes — twice in 35 min a Chrome extension-host restart killed the runner mid-open and the batch surrendered wholesale; now only a proven-alive runner earns ONE bounded fresh attempt, max 2/batch).

## ON-mode findings (12:44–13:55 EDT, peak clinic — Athena tab HIDDEN throughout)
- Pull #1 (17m12s): 14/17 patients fully body-complete — **93 new verified bodies**, every one identityVerified + bound to its patient, ZERO duplicate pairs, zero wrong-patient, one same-date verified REFRESH (4→4 bodies, replaced text — the authoritative-refresh path). 3 honest refusals: 2× visit-bodies-incomplete + 1× same-frame-name-mismatch.
- Retry: same 3 refused. Pull #2 via __mlsSI.pull at PEAK clinic (31m): 8/17 complete, 9 refusals (6× visit-bodies-incomplete, 2× same-frame-name-mismatch, 1× visits-time-budget-exceeded) — zero bad data again.
- **Root cause proven by direct probe**: the twice-refused 1-encounter patient read PERFECTLY (expected 1 / parsed 1, complete receipt, 12s) minutes after his 1:00 PM intake finished. The failing charts are exactly the ones under LIVE documentation (imminent/being-seen appointments); athena's encounter surface genuinely will not yield a stable bound detail while staff edit it. The reader gates are CORRECT — complete on quiet charts, honest refusal + targeted retry on active ones. NOTE: the hidden-tab bodies lane WORKS on 3.0.2 (93 bodies with the Athena tab backgrounded) — the old foreground-only constraint no longer holds post-flip.
- Conclusion: "2 consecutive perfect ON pulls" requires a quiet-chart window (post-clinic). Scheduled for after ~16:00 EDT when the 15:20 last appointment clears.
- Retry lane at 14:10: 3→2 remaining (queue shrinks as charts quiet). Store at 14:20: 135 verified bodies, 0 pairs, 0 bad bindings, 15/17 scheduled patients body-complete.

## Prepared while waiting (no byte changes)
- **Release dry-run pin map** (3.0.1→3.0.2): extension-version.json ver×1; get-extension.html zip×2 + escaped×? + ver×3 + sha×1; ScribeFlow.html zip×2 ver×3; sw.js passthrough ver×2; _config.yml zip+sha+ver×2; inventory zip×1; checker SERVER_EXT_VERSION ×1 + immutable token 20260721chk301r1 in mls-connect ×2 + reload-helper test ×2 (new token pattern 20260722chk302r1); test pins: extension-package sha×1, public-publication-boundary zip×5/ver×10/sha×2 (incl. ESCAPED v3\.0\.1 forms), release-truth-boundary ver×2+sha×1. NOTE: immutable-satellite-loader-cache.test.js not found by that name — locate the actual satellite pin test before moving tokens.
- **wsg-1.2.0 Adam-only test-write gate (design, to implement as 3.0.3-WIP AFTER the 3.0.2 release so acceptance/release bytes stay frozen)**: checkTestWritePolicy currently hard-blocks ALL test-looking content in production. Owner-sanctioned change (goal /writeback): allow flaggedTest writes ONLY when the verified expectedPatient identity is the designated test patient (nameKey 'adam j schaeffer' AND DOB 03/24/2006, MRN 7833832 when present); refuse every other patient (unchanged message). Scope stays note_text only (PREVIEW_ONLY_CATEGORIES untouched), final actions remain blocked by all layers (wsg content-script interceptor + policy gate + driver clickOnce + human-only Sign&Save). Unit tests: Adam+test-content → proceed; any other patient+test-content → blocked; Adam+order-like → still preview-only; non-test content path unchanged. Local Adam record: id mr85n5sdkd6o, 7 index rows, 0 bodies; encounter existence gets verified live at write time (write floor = encounter DOCUMENTATION view).

## Evening arc (20:00-21:00 EDT)
- **E1 (b492 adaptive) outcome**: main pass 16/16 in ~14 min with 8 sweepable failures; sweep pass 1 recovered 5, pass 2 recovered 1, pass 3 was re-reading the last 2 in-use charts at ~40 min when the pull tab (256593515) was closed externally (owner tidying tabs). Receipt lost with the tab; saves persisted. Store after: 128 bodies / 15 of 17 scheduled patients body-complete / 0 duplicate pairs / 0 bad bindings. Not countable as a complete ON pass (external interruption, not a reader failure). p_sched_1rod3o5 still 0 bodies.
- **3.0.2 RELEASED and live-verified twice** (QA session deployed b493 = 9800716 carrying the full release train; this session independently byte-verified): app-version b493, feed version 3.0.2, served zip sha256 b3012cb8...2dbc2 exact (376,230 bytes), get-extension + ScribeFlow Settings on 3.0.2 with zero stale 3.0.1 refs. Owner showcase item 1 DONE.
- **si-1.9.4 cumulative sweep progress** (owner: "when the bar resets it makes it seem like nothing was done"): the pull bar parses "N of M" from statuses, so a sweep sub-batch reporting "1 of 2" visually discarded 14 finished charts. Sweep sub-batches now report (base + i) of the ORIGINAL total plus an "(automatic re-check)" marker, and the sweep announcement leads with held progress ("14 of 16 charts finished - re-checking 2..."; count-free wording when zero finished so the bar never paints 0/N). Pipeline suite: behavioral status capture + static pins. Gate-green standalone.
- **oa-1.0.0 proposed-order acceptance** (owner showcase item 3): root causes - (a) the unified review card had NO accept control, only the "Suggestion only..." refusal; (b) acceptAiSuggestion silently no-ops when the parsed type has no ORDER_DEFS builder form, so some accepts never recorded; (c) suggestion text that never exactly matched an accepted order's _src kept re-rendering as unaccepted. Fix: review-and-accept button on suggestion rows (wf2-2.1.0) -> app hook _athenaAcceptProposedOrder (both ScribeFlow files; idempotent by text; stamps legacy _ai orders in place; accepts type-less suggestions) -> review reopens with the item as an accepted reviewed draft and fresh hashes. Acceptance is a recorded review decision only; placement stays human-in-Athena. 10 writeflow-adjacent suites + orders contract green.
- **wf2-2.2.0 seamless Write** (owner showcase item 2): the unified review now auto-opens the exact identity-verified chart when the probe refuses with a whitelisted not-open reason (context-unverified/context-mismatch), using the SAME SearchOpen verb the one-click lane proved in wf2-2.0.0 (identity frozen: name+DOB+MRN, appointment id + schedule date when present; row identity verified BEFORE the chart opens), then re-probes once. One auto-open per review; identity/token/tab failures never auto-open; the single human Confirm & write click is unchanged. Works against LIVE ext 3.0.2 - the mlsAppSearchOpenPatient content bridge ships in the released package; NO extension change needed.
- **wsg-1.2.0 DECISION - carve-out REJECTED, lane closed**: tests/test-content-production-boundary.test.js deliberately pins that shipped guard/worker/bridge bytes NEVER embed the designated test patient (name or MRN 7833832) and that even an MRN-matching patient stays blocked for test-flagged content ("production builds never carry a named patient/MRN allowlist"). A hardcoded Adam allowlist would ship a bypass in every customer install. Writeback acceptance instead runs the REAL product lane on the designated Adam record with realistic clinical content (explicitly authorized by the goal), exercising every gate the demo uses. No 3.0.3 needed for current goals; live 3.0.2 stays the stable demo release.
- **E2 ON pull launched 20:44 EDT** on tab 256593609 (b493, pullVisitBodies=1, one Athena tab): receipt capture in __accE2Done, statuses in __accE2Status, baseline 128 bodies / 0 pairs / 0 badBinding.

## E2/E3 ON pulls + b494 live proofs (20:44-23:10 EDT)
- **E2 (b493, 20:44, 42 min)**: honest partial. Schedule 16/16, history 16/16 processed, 13 sweepable failures at main-pass end, sweep pass 1 recovered 3 -> 10 failures, budget exhausted at the 45-min cap. Receipt field-mapping lesson: capture the FULL receipt JSON in .then (the receipt object is opaque afterwards).
- **b494 deployed between pulls** (e4398ca; union incl. QA lane per their coordination note; 272 suites green twice). Live-verified: __MLS_AV=b494, wf2-2.2.0 installed, _athenaAcceptProposedOrder function present.
- **E3 (b494, 22:12, 45 min)**: honest partial. Schedule 16/16, history 16/16, 11 sweepable at main-pass end, sweep pass 1 recovered 2 -> 9 failures, budget exhausted, NOT timedOut. FULL reasons (first complete histogram tonight): visit-bodies-incomplete x4, visits-time-budget-exceeded x2, same-frame-name-mismatch x3 - all slowness/stale-frame protection classes, ZERO identity/data errors.
- **si-1.9.4 LIVE-VERIFIED on E3**: sweep announced "5 of 16 charts finished - re-checking 11 in-use charts (automatic pass 1)..." and counters continued "6 of 16 (automatic re-check)" ... "16 of 16 (automatic re-check)" - no reset to 1-of-N. (E2 on b493 showed the OLD behavior "Reading verified history 1 of 13..." right after 16/16 - the before/after pair is on record in __accE2Status/__accE3Status.)
- **Environment finding**: evening Athena served several encounter reads at 200-350s each (10x normal; quiet clinic, hidden tab, extension bytes unchanged since morning) - the failures are environmental slowness, not reader defects. 45-min cap + honest partial + retry is the designed behavior. ON x2 complete-pull acceptance moves to a responsive-Athena window (daytime active clinic per goal).
- **HUSK GUARD LIVE CATCH (b490 guard, 22:09 EDT)**: on the b494 reload, loadPatientsFromServer's hydration guard fired for p_sched_ycpoio ({via:'hydrate'}) - the SERVER mirror still holds the 15:59 husk copy and tried to replace the 3 locally recovered bodies; the guard kept them. Implication: the server mirror for ycpoio (and p_sched_1rod3o5) needs healing by a successful body-complete re-mirror; until then every hydrate re-fights the husk. Store after E2+E3: 128 bodies, 0 pairs, 0 badBinding, 15/17 sched patients with bodies, 1rod3o5 still 0 (failed again both pulls - it is one of the slow/refusing charts).
- **R1 retry lane launched 23:10** on E3's exact 9 failures (retryFailedHistory from the captured receipt) - tests "retry retries only failures" acceptance item + may converge the day.

## Overnight close-out (23:10 Jul-22 - 00:45 Jul-23 EDT)
- **R1 retry lane PROVEN on E3s receipt**: retried exactly the 9 failures (nothing else), converged 3 (day trajectory 16 -> 9 -> 6 outstanding), honest partial on the same slow-chart cohort; si-1.9.4 cumulative counters verified inside the retry job too ("2 of 9 charts finished - re-checking 7...").
- **Seamless-open leg PROVEN LIVE on the designated test patient**: mlsAppSearchOpenPatient {Adam J Schaeffer, 03/24/2006, 7833832} -> {ok:true, opened:true, via:'findpatient', candidates:1} - identity verified on the result row BEFORE the chart opened. Full write acceptance BLOCKED on owner: Adam has NO encounters (order groups/patient cases only), and the write floor is the encounter documentation view (check-in = human-only).
- **oa-1.0.0 DOM-verified live on b494**: unified review rendered a suggestion row WITH the "Accept this proposed order" button + "will not be asked again" copy; dialog closed clean (render-only check; recorder semantics proven by the runtime suite).
- **Month coverage lane (includeHistory:false)**: M1 = 23 July days in 3m12s - 19 complete, PERFECT reconciliation (223/223 appointments accounted; 46 created, 128 repaired, 49 already present). Retry-days lane exercised (smp-1.2.0 allow-list): recovered 2026-07-06 fully (24/24, 23 created - an entire missed clinic day found and imported). Systemic breaker fired correctly when consecutive days failed identically.
- **NEW REPRODUCIBLE EXTENSION BUG (3.0.3 item)**: "N listed / N-1 verified" days are NOT slowness - an "8:00 AM FROZEN" block row (2026-07-08, POSM CL Kennett Square) enters candidateCount AND unnamedCount in background.js (~6620-6660): receipt {countStrategy:'legacy-grid-candidate-rows', expected:14, candidate:14, parsed:13, unnamed:1, domValidRows:13} - the completeness gate (parsed>=expected && unnamed==0) can NEVER pass on such days. 2026-07-10 (1/0) + 2026-07-18 (no readable rows) are the same family (block-only days). Fix: classify frozen/blocked/hold rows without patient identity as schedule furniture (exclude from candidateCount + unnamedCount).
- **Post-midnight view flip**: athena rolled the schedule surface to Week-by-Department at midnight; day pulls read "no readable rows" until a goto restored the day surface. The engines gotoDate recovered it (manual dispatch); worth an automatic pre-flight goto when the day surface is unreadable (queued polish).
- **ON-mode history pull (includeHistory:true) at midnight**: 07-01 showed 17/19 sweepable at pass 1 - three-week-old charts are NOT "in use" at midnight; confirms the failure classes are Athena-side slow/stale encounter rendering, not live documentation. Stopped the full-history month grind (bounded value against a degraded backend); daytime rerun scheduled.
- Store integrity at close: unchanged, clean (0 pairs, 0 badBinding, husk guard holding ycpoio bodies against the stale server mirror on every hydrate).

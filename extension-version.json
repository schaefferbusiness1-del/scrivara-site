{
  "version": "1.48",
  "url": "https://mlsscribe.com/get-extension.html",
  "notes": "v1.48 — RELIABLE all-providers pull. New structure-based scrape: reads athenaOne appointments by their unique appointment IDs inside each provider's schedule column (instead of guessing by screen position), so it dedupes perfectly and assigns every appointment to the correct doctor even on a busy, scrolled day grid. Falls back to the previous method if the layout differs. Also keeps v1.47 auto-navigate (now targets 'View Calendar') + config-tunable selectors."
}

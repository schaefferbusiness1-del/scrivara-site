{
  "version": "2.9.21",
  "url": "https://mlsscribe.com/get-extension.html",
  "notes": "v2.9.21 - Athena's legacy day grid now reads every explicit appointment row without a fragile text-length limit, excludes OPEN capacity slots, reconciles duplicate sort-mode containers, preserves provider identity and detailed visit reasons, and fails closed on unresolved patients. The packaged reader is regression-tested against the live 40-row shape: two copies of 18 real patients plus two OPEN slots per copy must resolve to exactly 18 patients."
}

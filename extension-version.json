{
  "version": "2.9.4",
  "url": "https://mlsscribe.com/get-extension.html",
  "notes": "v2.9.4 - Editorial Calm UI for the in-page panel, athena overlay and toolbar popup (deep green/mint, calm loading bar, reduced-motion safe) + repo sources synced to the certified v2.9.3 background/content (targetChartMatches write guard, _reasonS appt-type capture). Prior: v2.9.1 - Same feature set as v2.09, republished with a Chrome-safe version string (Chrome parses each dot-segment as a plain integer, so 2.09 displayed as 2.9; 2.9.1 is unambiguous and sorts above it). Includes the unified verified write driver (mlsAppWriteV2) with structural drawer detection, frame adoption, shadow-piercing note-editor targeting, read-only verify mode, and hard order-class hold (forcedHeld); the one-click Write to Athena flow with preview-only suggested-order chips; and the v2.04 settle-poll + foreground-for-read visits reader with the future-rows-are-bookings rule, identity de-race, self-healing keep-alive, and the order-never-submit policy."
}

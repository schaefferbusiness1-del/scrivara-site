{ "version": "1.39", "url": "https://mlsscribe.com/get-extension.html", "notes": "MLS Assist v1.39 - Op-note writeback to the EXACT athenaOne location, plus the v1.38 signed-in-tab + connection-truth fixes. NEW: Write op-note (and Simple-view Finish) drives PE > Procedure Documentation > adds the chosen procedure template (e.g. Injection Generic Template), then the verified paste ERASES the template skeleton and INSERTS the op-note - behind the name+DOB patient-match gate, and it NEVER clicks Save or Sign (the doctor signs). You can change WHERE each thing is written per doctor by telling the MLS Assistant in plain English (e.g. put injections under Physical Exam). Includes v1.38: prefer your real signed-in athenaOne dashboard over a stray sign-in tab, and honest connection status. Download and reload at chrome://extensions to update." }

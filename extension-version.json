{
  "version": "2.9.20",
  "url": "https://mlsscribe.com/get-extension.html",
  "notes": "v2.9.20 - Athena day pulls now wait for virtual rows to finish rendering, parse Last/First names canonically, reconcile hidden provider copies without collapsing real cross-provider visits, verify every requested scroll position, and fail closed on unresolved rows. The packaged reader is regression-tested against the live 38-rendered-copy to 18-appointment schedule shape, with an optional PHI-safe redacted support receipt for live diagnosis."
}

{
  "version": "2.9.38",
  "url": "https://mlsscribe.com/get-extension.html",
  "notes": "v2.9.38 - Encounter bodies read again on athenaOne's newer streamlined chart layout: when a verified encounter row exposes no known expand control, the reader now tries any safe clickable inside that row and finally the row itself (destructive controls are always refused), while the existing frame-proof chain still decides what counts as a real body. Includes v2.9.37 (retryable expand failures + diagnostics, no console errors on other sites)."
}

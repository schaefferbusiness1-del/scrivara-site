{
  "version": "1.47",
  "url": "https://mlsscribe.com/get-extension.html",
  "notes": "v1.47 — (1) All-providers scrape: fires a scroll event + waits so athenaOne's virtualized day grid renders every provider's column before reading (fixes only 2-4 doctors coming through). (2) NEW auto-navigate: if the schedule grid isn't showing when you Pull, it opens athenaOne's Calendar menu and clicks the day view (e.g. Today's Appointments), then pulls — read-only navigation with a hard deny-list on any write/sign/bill/book control. Selectors, nav labels, and scroll timing are all tunable from the hosted config with no reinstall."
}

{
  "version": "1.46",
  "url": "https://mlsscribe.com/get-extension.html",
  "notes": "v1.46 — schedule pull now SCROLLS across the whole athenaOne day grid to capture EVERY provider's appointments (fixes the 'only 2-3 doctors' bug caused by Athena virtualizing off-screen columns). Keeps v1.45 config-driven selectors + clean names + schedule-date capture. Falls back to the single-view read if no scroller is found."
}

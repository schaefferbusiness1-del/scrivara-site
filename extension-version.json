{
  "version": "3.0.105",
  "minChrome": 116,
  "notes": "v3.0.105 - Pull reliability: on the athenaOne dashboard schedule, where each provider is a heading stacked above that provider's rows, every row now carries its heading's provider (v3.0.104 handled only side-by-side columns and table headings), so a provider-scoped month pull files those days instead of refusing them. Everything from v3.0.104 remains. Requires Chrome 116+."
}

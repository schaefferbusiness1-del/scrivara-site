{
  "version": "2.9.33",
  "url": "https://mlsscribe.com/get-extension.html",
  "notes": "v2.9.33 - Order-group rows are no longer silently skipped during history pulls: each administrative order group row is filed as its own dated index entry (outside the verified clinical chain, so every existing completeness proof is unchanged), and the pull narrates it instead of appearing to skip every other visit. Includes the v2.9.32 speed and Mac changes."
}

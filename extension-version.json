{
  "version": "3.0.26",
  "url": "https://mlsscribe.com/get-extension.html",
  "notes": "v3.0.26 — Finishes 3.0.25. The day-navigation repair is now actually ENFORCED: the one place that acts on a restored schedule date refuses to open an appointment on a day the extension could not confirm, instead of accepting its own echo. Day navigation still reports the day athenaOne is actually showing, reading the selected day tab rather than only the page heading. Reading the open chart identity cannot hang without answering. Two permissions the extension never used (audio capture, offscreen documents) remain removed. Requires Chrome 116+."
}

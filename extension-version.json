{
  "version": "3.0.47",
  "minChrome": 116,
  "notes": "v3.0.47 - When the chart reader cannot find the encounter list it now says exactly which athena surface answered (and whether the Visits pane was even on screen) instead of a cryptic code, so a failed history read tells you what to fix. A stuck or cancelled pull can no longer wedge the athenaOne signed-in checklist row - that check now runs lease-free. Everything from v3.0.46 remains. Requires Chrome 116+."
}

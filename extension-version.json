{
  "version": "2.9.31",
  "url": "https://mlsscribe.com/get-extension.html",
  "notes": "v2.9.31 - Multiple Athena tabs no longer block the supervised write lane or teaching. The write probe now checks every signed-in Athena tab and proceeds only when exactly one tab verifies the exact patient and encounter; duplicates and zero matches still fail closed, and the confirmed write can only land in the single verified tab. Teach destination watches the Athena tab you used most recently and says so when several are open. Everything else unchanged from v2.9.30."
}

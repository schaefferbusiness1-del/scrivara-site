{
  "version": "2.01",
  "url": "https://mlsscribe.com/get-extension.html",
  "notes": "v2.01 - Visits reader honesty: the Visits-and-Cases reader can no longer report an empty history unless athenaOne explicitly shows zero events (a declared event count with unreadable rows is a retryable refusal, never a silent empty); adds menu-close recovery for the clientsummary chart layout. Keeps the tab hand-off panel with pinning + self-healing keep-alive, the focus guardian, verified per-section write bridge, patient-identity gates, junk-frame guards and week-strip date navigation."
}

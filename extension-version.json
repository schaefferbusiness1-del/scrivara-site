{
  "version": "1.47",
  "url": "https://mlsscribe.com/get-extension.html",
  "notes": "v1.47 — scroll-scrape now fires a scroll event + waits for each grid section to render, so it captures EVERY provider's appointments across the whole athenaOne day grid (fixes v1.46 getting 0/partial because programmatic scroll didn't trigger Athena's virtualized render). Config-driven selectors + clean names + schedule-date capture retained."
}

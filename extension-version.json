{
  "version": "2.04",
  "url": "https://mlsscribe.com/get-extension.html",
  "notes": "v2.04 - Visits reader: settle-poll waits for the row list to hydrate before deciding rows-not-parsed (fixes the first-try 'That Athena pull didn't work' banner); reads now foreground the athenaOne tab for the duration (hidden tabs never hydrate the pane's rows) and return you to MLS after; any row dated after today is treated as a booking, never filed as history (marker-less future rows, e.g. scheduled PT sessions and follow-ups, were previously mis-filed). Keeps the v2.02 PT/booking time-tolerant parser, identity de-race, tab hand-off panel, self-healing keep-alive, verified per-section write bridge, and the order-never-submit policy."
}

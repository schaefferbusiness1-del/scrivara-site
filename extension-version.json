{ "version": "1.28", "url": "https://mlsscribe.com/get-extension.html", "notes": "MLS Assist v1.28 — hardened verified typing. The reliable typing primitive now resolves the real editable field from a label/wrapper, clicks+focuses first, drives masked date fields and typeahead/autocomplete (types then selects the match), re-reads to confirm the text stuck and retries the alternate method, and makes autopilot STOP (not loop) on a stuck field. Fixes autopilot not filling the Name and date-of-birth fields. Content-aware routing and the mandatory patient-match safety gate are unchanged. Never saves or signs for you. Download and reload at chrome://extensions to update." }

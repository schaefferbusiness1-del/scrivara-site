{ "version": "1.10", "url": "https://mlsscribe.com/assist.html", "notes": "Much smarter autopilot: it now sees a precise numbered list of the real buttons/fields on the page and targets them by index, so it reliably finds things like the 'New patient' / + button and fills scheduling forms (name, date of birth, dropdowns) and clicks Create/Schedule. Run/Stop is fixed with a live status line. A bit slower per step, much more accurate. Reload at chrome://extensions to update." }

# Handoff — 2026-07-21 (b470 live, mls-v57)

## What broke for the owner today and what fixed it
1. **"Every pull fails: could not verify the existing MLS calendar"** → the tab's MLS session had EXPIRED (~1.5h server TTL); the token was present but dead, Athena still read fine, and the MLS calendar read 401'd every time with a misleading "connection" message. **Fixed (b470):** pull pre-flights `/api/me` and refuses up front as `signin-expired` ("Sign in to MLS again, then pull"); 401/403 on the calendar read classifies as signin-expired across the exact importer, the message maps, and the legacy import toast.
2. **Sign-in "glitches out" + clinical gate "blocked" + WALL of "Save not confirmed" banners** → one transient `/api/me` failure left the tab tokened-but-anonymous (`sf_u::_` namespace): gate fail-closed, store hydrated into the wrong bucket, and dozens of queued save-verifications re-read an empty store. **Fixed (b470):** refreshMe retries 3×; save-verify cancels queued verifications on any `mls:session-boundary`, suppresses itself while signed out / identity unresolved / gate visible, and collapses >3 warnings into one honest summary. Live-verified: the glitched tab reloaded into leeschaeffer41 with 1440 patients, no gate, zero banners.
3. **Grayed "On all sites"** → by design (scoped host permissions). Setup guide live on assist.html; decision + trade-offs in EXTENSION_3.0.x_GUIDE_2026-07-21.md. All-sites was NOT shipped — it fixes nothing and would add a scary store warning + review delay; flip deliberately if still wanted.
4. **"No retry failed histories button"** → it had NEVER been visible (inline `display:''` fell back to CSS `display:none`). Fixed + test-pinned; same fix for the Copy-error-report button.
5. **Bottom-right progress chip "gone"** → its old boot content was the removed silent auto-Athena read (the false "needs attention" bug). Boot hydration now registers as a real job in the chip. NOTE: the job store can load a beat after hydration starts; a small retry in `_hyStart` is queued for b471 so the chip shows on every boot.
6. **Pull button label** → "Pull today" / "Pull Wednesday, July 22" (owner order). "Full visit notes" defaults OFF (the slow fragile bodies lane is opt-in, persisted per account). Install-MLS-Assist banner no longer nags machines that HAVE the extension (fast boot beat the content script; second ping + any-extension-message withdrawal).

## Pull proven end-to-end (owner's demand "don't say done till tested")
Real clicks, doctor account: schedule 14/14 resolved across two departments, day marked complete (`schedImportIndexV1` all done, `schedImportDaysV1=["2026-07-21"]`), patients 1439→1440. The one unresolved row (Julia Grieco) resolved on retry exactly as the receipt advised. Failures in between were the multi-Athena-tab drive issue → **keep ONE signed-in Athena tab** (candidate for an in-app hint). History bodies stayed honestly incomplete (occluded-tab lane, now opt-in); headers/coverage landed. Mary Murray Young carries a real data-quality flag: 14 athenaOne name matches, none matching stored DOB 08/27/1954 — owner should eyeball her chart.

## Answers to owner questions
- **Fresh-account first pull:** an all-provider Day pull needs NO provider roster (code: `resolveProviderRequest allowAll:true, requireRosterForAll:false`) and BUILDS the roster — first pull IS the setup. Selected-provider and month pulls require the roster the first day pull creates; failure messages say exactly that. The schaefferbusiness failures were the expired-session chain, not a fresh-account defect.
- **Month pull:** works via Menu → Staff prep & Athena month pull for any rostered provider (same exact engine per frozen day; systemic-failure circuit breaker si-1.7.12; b449 proved 17/17 hands-off).
- **"Maybe history saved but reported missing":** checked directly — visit `detail` fields are genuinely empty when bodies fail; the banner is truthful. Headers/dates/types/coverage DO save.
- **Voice/Dictate/Assistant buttons "missing":** all six Quick Tools verified present on b470; they vanish only in the identity-limbo state (premium gates can't resolve an anonymous account) — fixed by the refreshMe retry.

## Still queued (do next)
1. **Extension 3.0.1 release train** (stale `v2.9.22 r4 diag` label fix; optional bodies watchdog tuning) — full release protocol in the extension guide. NOT shipped today on purpose: unreviewed extension churn mid-clinic-day is the wrong risk.
2. **b471 polish:** `_hyStart` retry so the boot chip always appears; consider an in-app "multiple Athena tabs" hint on nav-failed.
3. **Full visit walkthrough** (record → generate note → write-lane preview on a safe patient) — owner ordered it; needs a dedicated pass. Write floor stays review-first; Sign/Save is human-only.
4. Owner queue unchanged: stale server dup rows [3,393658,393402,394437,394443,394445] + PULL-004 + dup-merge [393707,393710] await explicit OK; other devices still on ext 2.9.x; SERPER_API_KEY.

## Where things are
- Site repo working copy: `dispatch-work/claude-commercial-20260717` (branch pushed to main; b470 live, CACHE mls-v57; suite 253 green).
- Evidence log: `tests/live-e2e-artifacts/2026-07-20-ui-quality-acceptance.md` (pull proofs + this round appended).
- Extension truth: `EXTENSION_3.0.x_GUIDE_2026-07-21.md` (same folder as this file).
- Backend: manual "Deploy latest commit" ONLY (auto-deploy dead); never deploy while the owner is mid-pull.
